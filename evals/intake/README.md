# Eval Intake

Proposal area for `ATM10-Agent` eval pressure.

Keep proposals public-safe. Accepted cases must move into an ATM10-owned suite
with a deterministic local test reference and protected-behavior ID.

This directory is empty until a concrete project-local need is accepted.
