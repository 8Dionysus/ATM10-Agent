# SESSION_2026-03-02

Brief summary of the session from **2026-03-02** (`G2.1` go/no-go governance layer for `fail_nightly`).

## What has been done

* Closed `G2.1` follow-up on governance for SLA policy switch:
  * added `scripts/check_gateway_sla_fail_nightly_governance.py` with contract
    `gateway_sla_fail_nightly_governance_v1`;
  * governance rules formalized:
    * source: `gateway_sla_fail_nightly_readiness_v1` history,
    * promotion rule: `>=3` in a row `readiness_status=ready`,
    * additional conditions `go`: latest=`ready`, `invalid_or_mismatched_count=0`,
    * recommendation switch surface: `nightly_only`.
* Added new test suite:
  * `tests/test_check_gateway_sla_fail_nightly_governance.py`:
    * `go` happy path,
    * `hold` (latest not_ready),
    * `hold` (streak < threshold),
    * criteria mismatch handling,
    * invalid/corrupt input handling,
    * no valid summaries -> `status=error`,
    * exit policy (`report_only|fail_if_not_go`),
    * CLI help smoke.
* Nightly workflow `.github/workflows/gateway-sla-readiness-nightly.yml` expanded:
  * cache restore/save now includes `runs/nightly-gateway-sla-readiness`,
  * added governance step (`report_only`),
  * added summary section `Gateway SLA Fail-Nightly Governance`,
  * upload artifacts extended by `runs/nightly-gateway-sla-governance`.
* Synchronized source-of-truth docs:
  * `docs/RUNBOOK.md`,
  * `docs/DECISIONS.md`,
  * `TODO.md`.

## Tests

Targeted:

* `python -m pytest tests/test_check_gateway_sla_fail_nightly_governance.py` -> **9 passed**

Full regression:

* `python -m pytest` -> **285 passed**

## Status after the session

* `G2` readiness and `G2.1` governance instrumentation are closed in staged mode (without hard-gate switch).
* Production switch on `critical_policy=fail_nightly` is still not enabled:
  * it requires accumulation of real nightly history and passing governance-gate (`3` ready in a row).
* `pytest.yml` remains in `signal_only`; potential switch surface is fixed as `nightly_only`.

---

## Update (same date): Runtime hardening + dependency profiles/audit

### What has been done

* Completed runtime hardening/fix waves via gateway/voice/tts:
  * `gateway_v1_http_service`: timeout-path via lifecycle executor, fast `504`, `lifespan` migration (FastAPI `on_event` -> `lifespan`).
  * `phase_a_smoke`: strict-failure path always saves `run.json` and `response.json` before re-raise.
  * `voice_runtime_service`: request payload limits + mapping `payload_too_large|payload_limit_exceeded -> HTTP 413`.
  * `tts_runtime_service` + `src/agent_core/tts_runtime.py`: true streaming path (`/tts_stream`) without full pre-buffer and correct bool parsing `service_voice`.
* Added dependency profiles:
  * `requirements-voice.txt`
  * `requirements-llm.txt`
  * `requirements-export.txt`
  * `requirements-audit.txt`
  * base `requirements.txt` updated with explicit `numpy`.
* Added machine-readable dependency audit entrypoint:
  * `scripts/dependency_audit.py`
  * artifacts: `run.json`, `dependency_inventory.json`, `dependency_findings.json`, `security_audit.json`, `summary.md`.
* CI (`.github/workflows/pytest.yml`) expanded report-only dependency audit with steps (jobs `test` + `smoke`) and upload artifact `dependency-audit-report`.
* Tests added:
  * `tests/test_requirements_profiles.py`
  * `tests/test_dependency_audit.py`

### Tests (updated snapshot)

Targeted:

* `python -m pytest tests/test_gateway_v1_http_service.py tests/test_phase_a_smoke.py tests/test_tts_runtime_service.py tests/test_voice_runtime_service.py tests/test_tts_runtime.py tests/test_requirements_profiles.py tests/test_dependency_audit.py` -> **73 passed**

Dependency audit smoke:

* `python scripts/dependency_audit.py --runs-dir runs --policy report_only --with-security-scan true` -> **exit 0**, artifacts: `runs/20260302_014021-dependency-audit/`

Full regression:

* `python -m pytest` -> **309 passed**

---

## Update (same date): G2.2 progress-layer for fail_nightly decision tracking

### What has been done

* Added new progress checker:
  * `scripts/check_gateway_sla_fail_nightly_progress.py`
  * summary contract: `gateway_sla_fail_nightly_progress_v1`
  * source: readiness + governance history (`**/readiness_summary.json`, `**/governance_summary.json`)
  * machine-readable progress fields: `remaining_for_window`, `remaining_for_streak`,
    `valid_count`, `invalid_or_mismatched_count`, `latest_decision_status`.
* Tests added:
  * `tests/test_check_gateway_sla_fail_nightly_progress.py`:
    * `go` happy path,
    * `hold` (insufficient window),
    * `hold` (streak below threshold),
    * invalid/mismatched history handling,
    * no valid readiness history -> `status=error`,
    * exit policy `fail_if_not_go`,
    * CLI help smoke.
  * `tests/test_gateway_sla_readiness_nightly_workflow.py`:
    * records that nightly workflow contains progress step, summary and cache/artifact wiring.
* Nightly workflow `.github/workflows/gateway-sla-readiness-nightly.yml` expanded:
  * added step `Progress - Gateway SLA fail_nightly decision progress (report_only)`;
  * added summary section `Gateway SLA Fail-Nightly Progress`;
  * cache restore/save expanded `runs/nightly-gateway-sla-governance`, `runs/nightly-gateway-sla-progress`;
  * upload artifacts extended by `runs/nightly-gateway-sla-progress`.
* Synchronized source-of-truth docs:
  * `docs/RUNBOOK.md` (section `G2.2`),
  * `docs/DECISIONS.md`,
  * `TODO.md`,
  * `MANIFEST.md`.

### Tests (updated snapshot)

Targeted:

* `python -m pytest tests/test_check_gateway_sla_fail_nightly_progress.py tests/test_gateway_sla_readiness_nightly_workflow.py` -> **8 passed**
* `python -m pytest tests/test_check_gateway_sla_fail_nightly_progress.py tests/test_gateway_sla_readiness_nightly_workflow.py tests/test_check_gateway_sla_fail_nightly_readiness.py tests/test_check_gateway_sla_fail_nightly_governance.py` -> **26 passed**

Full regression:

* `python -m pytest` -> **332 passed**

---

## Update (same date): G2.post Streamlit progress visibility (optional-source policy)

### What has been done

* `scripts/streamlit_operator_panel.py` extended optional data-layer for nightly fail_nightly progress sources:
  * `runs/nightly-gateway-sla-readiness/readiness_summary.json`
  * `runs/nightly-gateway-sla-governance/governance_summary.json`
  * `runs/nightly-gateway-sla-progress/progress_summary.json`
* Added helper functions for loading/normalizing contracts:
  * `gateway_sla_fail_nightly_readiness_v1`
  * `gateway_sla_fail_nightly_governance_v1`
  * `gateway_sla_fail_nightly_progress_v1`
* The `Gateway fail_nightly progress` block with key fields has been added to the `Latest Metrics` tab:
  * `readiness_status`, `latest_ready_streak`, `decision_status`
  * `remaining_for_window`, `remaining_for_streak`
  * `target_critical_policy`, `reason_codes`
* Implemented tolerant rendering:
  * lack of optional sources -> `not available yet`;
  * broken optional JSON/contract mismatch -> warning in UI without crash.
* `scripts/streamlit_operator_panel_smoke.py` updated:
  * `streamlit_smoke_summary_v1` is supplemented with fields `required_missing_sources`, `optional_missing_sources`;
  * legacy `missing_sources` saved as required-only alias for backward compatibility;
  * `status=error` is still only for startup/mobile/required missing sources.
* Updated tests:
  * `tests/test_streamlit_operator_panel.py`
  * `tests/test_streamlit_operator_panel_smoke.py`
* Synchronized docs:
  * `docs/RUNBOOK.md`
  * `docs/DECISIONS.md`
  * `TODO.md`

### Tests (updated snapshot)

Targeted:

* `python -m pytest tests/test_streamlit_operator_panel.py tests/test_streamlit_operator_panel_smoke.py` -> **29 passed**

Smoke validation:

* `python scripts/streamlit_operator_panel_smoke.py --panel-runs-dir runs --runs-dir runs/ci-smoke-streamlit --summary-json runs/ci-smoke-streamlit/streamlit_smoke_summary.json --gateway-url http://127.0.0.1:8770 --startup-timeout-sec 45 --viewport-width 390 --viewport-height 844 --compact-breakpoint-px 768` -> **exit 0** (`status=ok`, `missing_sources_count=0`, `optional_missing_sources_count=3`)

Full regression:

* `python -m pytest` -> **337 passed**
