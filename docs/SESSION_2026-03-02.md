# SESSION_2026-03-02

Краткий конспект сессии от **2026-03-02** (`G2.1` go/no-go governance layer для `fail_nightly`).

## Что сделано

* Закрыт `G2.1` follow-up по governance для SLA policy switch:
  * добавлен `scripts/check_gateway_sla_fail_nightly_governance.py` с контрактом
    `gateway_sla_fail_nightly_governance_v1`;
  * governance rules formalized:
    * source: `gateway_sla_fail_nightly_readiness_v1` history,
    * promotion rule: `>=3` подряд `readiness_status=ready`,
    * дополнительные условия `go`: latest=`ready`, `invalid_or_mismatched_count=0`,
    * recommendation switch surface: `nightly_only`.
* Добавлен новый тестовый набор:
  * `tests/test_check_gateway_sla_fail_nightly_governance.py`:
    * `go` happy path,
    * `hold` (latest not_ready),
    * `hold` (streak < threshold),
    * criteria mismatch handling,
    * invalid/corrupt input handling,
    * no valid summaries -> `status=error`,
    * exit policy (`report_only|fail_if_not_go`),
    * CLI help smoke.
* Расширен nightly workflow `.github/workflows/gateway-sla-readiness-nightly.yml`:
  * cache restore/save теперь включает `runs/nightly-gateway-sla-readiness`,
  * добавлен governance step (`report_only`),
  * добавлен summary section `Gateway SLA Fail-Nightly Governance`,
  * upload artifacts расширен `runs/nightly-gateway-sla-governance`.
* Синхронизированы source-of-truth docs:
  * `docs/RUNBOOK.md`,
  * `docs/DECISIONS.md`,
  * `TODO.md`.

## Тесты

Targeted:

* `python -m pytest tests/test_check_gateway_sla_fail_nightly_governance.py` -> **9 passed**

Full regression:

* `python -m pytest` -> **285 passed**

## Статус после сессии

* `G2` readiness и `G2.1` governance instrumentation закрыты в staged-режиме (без hard-gate switch).
* Production switch на `critical_policy=fail_nightly` по-прежнему не включен:
  * требуется накопление реальной nightly истории и прохождение governance-gate (`3` ready подряд).
* `pytest.yml` остается в `signal_only`; потенциальный switch surface зафиксирован как `nightly_only`.

---

## Update (same date): Runtime hardening + dependency profiles/audit

### Что сделано

* Завершены runtime hardening/fix волны по gateway/voice/tts:
  * `gateway_v1_http_service`: timeout-path через lifecycle executor, fast `504`, `lifespan` migration (FastAPI `on_event` -> `lifespan`).
  * `phase_a_smoke`: strict-failure path всегда сохраняет `run.json` и `response.json` перед re-raise.
  * `voice_runtime_service`: request payload limits + mapping `payload_too_large|payload_limit_exceeded -> HTTP 413`.
  * `tts_runtime_service` + `src/agent_core/tts_runtime.py`: true streaming path (`/tts_stream`) без полного pre-buffer и корректный bool parsing `service_voice`.
* Добавлены dependency profiles:
  * `requirements-voice.txt`
  * `requirements-llm.txt`
  * `requirements-export.txt`
  * `requirements-audit.txt`
  * базовый `requirements.txt` обновлен явным `numpy`.
* Добавлен machine-readable dependency audit entrypoint:
  * `scripts/dependency_audit.py`
  * artifacts: `run.json`, `dependency_inventory.json`, `dependency_findings.json`, `security_audit.json`, `summary.md`.
* CI (`.github/workflows/pytest.yml`) расширен report-only dependency audit шагами (jobs `test` + `smoke`) и upload artifact `dependency-audit-report`.
* Добавлены тесты:
  * `tests/test_requirements_profiles.py`
  * `tests/test_dependency_audit.py`

### Тесты (updated snapshot)

Targeted:

* `python -m pytest tests/test_gateway_v1_http_service.py tests/test_phase_a_smoke.py tests/test_tts_runtime_service.py tests/test_voice_runtime_service.py tests/test_tts_runtime.py tests/test_requirements_profiles.py tests/test_dependency_audit.py` -> **73 passed**

Dependency audit smoke:

* `python scripts/dependency_audit.py --runs-dir runs --policy report_only --with-security-scan true` -> **exit 0**, artifacts: `runs/20260302_014021-dependency-audit/`

Full regression:

* `python -m pytest` -> **309 passed**

---

## Update (same date): G2.2 progress-layer for fail_nightly decision tracking

### Что сделано

* Добавлен новый progress checker:
  * `scripts/check_gateway_sla_fail_nightly_progress.py`
  * summary contract: `gateway_sla_fail_nightly_progress_v1`
  * source: readiness + governance history (`**/readiness_summary.json`, `**/governance_summary.json`)
  * machine-readable progress поля: `remaining_for_window`, `remaining_for_streak`,
    `valid_count`, `invalid_or_mismatched_count`, `latest_decision_status`.
* Добавлены тесты:
  * `tests/test_check_gateway_sla_fail_nightly_progress.py`:
    * `go` happy path,
    * `hold` (insufficient window),
    * `hold` (streak below threshold),
    * invalid/mismatched history handling,
    * no valid readiness history -> `status=error`,
    * exit policy `fail_if_not_go`,
    * CLI help smoke.
  * `tests/test_gateway_sla_readiness_nightly_workflow.py`:
    * фиксирует, что nightly workflow содержит progress step, summary и cache/artifact wiring.
* Расширен nightly workflow `.github/workflows/gateway-sla-readiness-nightly.yml`:
  * добавлен step `Progress - Gateway SLA fail_nightly decision progress (report_only)`;
  * добавлен summary section `Gateway SLA Fail-Nightly Progress`;
  * cache restore/save расширен `runs/nightly-gateway-sla-governance`, `runs/nightly-gateway-sla-progress`;
  * upload artifacts расширен `runs/nightly-gateway-sla-progress`.
* Синхронизированы source-of-truth docs:
  * `docs/RUNBOOK.md` (секция `G2.2`),
  * `docs/DECISIONS.md`,
  * `TODO.md`,
  * `MANIFEST.md`.

### Тесты (updated snapshot)

Targeted:

* `python -m pytest tests/test_check_gateway_sla_fail_nightly_progress.py tests/test_gateway_sla_readiness_nightly_workflow.py` -> **8 passed**
* `python -m pytest tests/test_check_gateway_sla_fail_nightly_progress.py tests/test_gateway_sla_readiness_nightly_workflow.py tests/test_check_gateway_sla_fail_nightly_readiness.py tests/test_check_gateway_sla_fail_nightly_governance.py` -> **26 passed**

Full regression:

* `python -m pytest` -> **332 passed**
