# SESSION_2026-02-23

Краткий конспект сессии от **2026-02-23**.

## Что сделано

* Выполнен аудит актуальных Markdown-документов проекта:
  * `README.md`, `MANIFEST.md`, `TODO.md`, `PLANS.md`
  * `docs/RUNBOOK.md`, `docs/DECISIONS.md`, `docs/SOURCE_OF_TRUTH.md`
  * `docs/ARCHIVED_TRACKS.md`, `docs/QWEN3_MODEL_STACK.md`
  * последние snapshots: `docs/SESSION_2026-02-20.md`, `docs/SESSION_2026-02-22.md`
* Подтвержден фактический baseline тестов:
  * команда: `python -m pytest`
  * результат: `136 passed`
* Синхронизирован `TODO.md`:
  * status snapshot обновлен на `2026-02-23`
  * добавлен `Session Focus (2026-02-23)` с приоритетами текущей сессии

## План текущей сессии

1. M6.2: определить следующий practical шаг после `automation_dry_run` без перехода к real input events.
2. KAG guardrail: зафиксировать canonical benchmark profile (`sample + hard`, `--warmup-runs 1`) и согласовать пороги quality/latency.
3. CI hygiene: выбрать 1-2 runnable smoke-сценария для расширения CI без flaky-регрессий.
4. После изменений в docs/scripts выполнить `python -m pytest` и зафиксировать итог в session log.

## Ограничения и границы (подтверждены)

* WIP limit: максимум 3 активные задачи одновременно.
* Active voice path: `whisper_genai`; `qwen_asr`/`Qwen3-TTS` остаются archived/recoverable.
* Политика source-of-truth документов соблюдается:
  * execution-план в `TODO.md`
  * цели/DoD в `PLANS.md`
  * решения в `docs/DECISIONS.md`
  * команды в `docs/RUNBOOK.md`

## Update (same date, M6.2 action-plan contract)

* Уточнен контракт `automation_plan_v1` в `scripts/automation_dry_run.py`:
  * добавлена явная валидация `schema_version`
  * добавлена нормализация блока `intent` (`goal/priority/tags/constraints`)
  * добавлена проверка уникальности `action.id`
* Добавлены canonical demo scenarios:
  * `tests/fixtures/automation_plan_quest_book.json`
  * `tests/fixtures/automation_plan_inventory_check.json`
* Добавлены regression tests контракта:
  * позитивный fixture-path для `automation_plan_v1 + intent`
  * негативный кейс на unsupported `schema_version`
* Обновлены docs:
  * `docs/RUNBOOK.md` (M6 sample + fixture commands)
  * `docs/DECISIONS.md` (решение по контракту M6.2)
  * `TODO.md` (`M6.2` переведен в done, новый next-step `M6.3`)
* Финальный regression-check:
  * команда: `python -m pytest`
  * результат: `138 passed`

## Update (same date, M6.3 intent adapter)

* Добавлен новый dry-run adapter:
  `scripts/intent_to_automation_plan.py`.
* Контракт adapter:
  * вход: `automation_intent_v1`
  * выход: `automation_plan_v1`
  * поддерживаемые template-intents: `open_quest_book`, `check_inventory_tool`
* Artifact contract:
  * `runs/<timestamp>-intent-to-automation-plan/run.json`
  * `runs/<timestamp>-intent-to-automation-plan/automation_plan.json`
* Добавлены tests/fixtures:
  * `tests/fixtures/intent_open_quest_book.json`
* Добавлены regression tests:
  * `tests/test_intent_to_automation_plan.py` (`success`, `invalid intent_type`, `CLI --help`)
* Обновлены docs:
  * `docs/RUNBOOK.md` (M6.3 section + e2e chain command)
  * `docs/DECISIONS.md` (M6.3 decision log)
  * `TODO.md` (`M6.3` отмечен как done, `M6.4` вынесен в Now)
* Финальный regression-check:
  * команда: `python -m pytest`
  * результат: `141 passed`

## Update (same date, M6.4 unified smoke chain)

* Добавлен единый dry-run smoke entrypoint:
  `scripts/automation_intent_chain_smoke.py`.
* Цепочка выполнения:
  * `automation_intent_v1` -> `automation_plan_v1` (adapter)
  * `automation_plan_v1` -> `execution_plan` (`automation_dry_run`)
* Artifact contract:
  * `runs/<timestamp>-automation-intent-chain-smoke/run.json`
  * `runs/<timestamp>-automation-intent-chain-smoke/chain_summary.json`
  * `runs/<timestamp>-automation-intent-chain-smoke/automation_plan.json`
  * `runs/<timestamp>-automation-intent-chain-smoke/child_runs/*`
* Добавлены regression tests:
  * `tests/test_automation_intent_chain_smoke.py` (`success`, `invalid intent`, `CLI --help`)
* Обновлены docs:
  * `docs/RUNBOOK.md` (M6.4 section)
  * `docs/DECISIONS.md` (M6.4 decision log)
  * `TODO.md` (`M6.4` отмечен как done, `M6.5` вынесен в Now)

## Update (same date, M6.5 CI smoke expansion)

* Обновлен CI workflow:
  * `.github/workflows/pytest.yml` (job `smoke`)
* Добавлены 2 новых lightweight smoke-step:
  * `python scripts/automation_dry_run.py --plan-json tests/fixtures/automation_plan_quest_book.json --runs-dir runs/ci-smoke-automation-dry-run`
  * `python scripts/automation_intent_chain_smoke.py --intent-json tests/fixtures/intent_open_quest_book.json --runs-dir runs/ci-smoke-automation-chain`
* Выбранный подход для flaky-control:
  * fixture-only сценарии (без Docker/аудио/внешних сервисов)
  * deterministic dry-run path с artifact contract
* Синхронизированы docs:
  * `docs/RUNBOOK.md` (CI smoke section)
  * `docs/DECISIONS.md` (M6.5 decision log)
  * `PLANS.md` (G3 open task обновлен на stability/thresholds)
  * `TODO.md` (`M6.5` + CI hygiene отмечены как done)
* Локальная проверка новых CI smoke-команд:
  * `python scripts/automation_dry_run.py --plan-json tests/fixtures/automation_plan_quest_book.json --runs-dir runs/ci-smoke-automation-dry-run-local`
  * `python scripts/automation_intent_chain_smoke.py --intent-json tests/fixtures/intent_open_quest_book.json --runs-dir runs/ci-smoke-automation-chain-local`
  * обе команды завершились успешно и записали expected artifacts.

## Update (same date, KAG guardrail profile)

* Добавлен guardrail checker:
  `scripts/check_kag_neo4j_guardrail.py`.
* Зафиксированы canonical профили порогов:
  * `sample`: `recall>=1.0`, `mrr>=0.80`, `hit-rate>=1.0`, `latency_p95<=120ms`
  * `hard`: `recall>=1.0`, `mrr>=0.90`, `hit-rate>=1.0`, `latency_p95<=130ms`
* Добавлены regression tests:
  * `tests/test_check_kag_neo4j_guardrail.py`
* Runbook обновлен командами проверки guardrail на `eval_results.json`.

## Update (same date, M6.6 automation smoke acceptance thresholds)

* Добавлен CI contract checker:
  `scripts/check_automation_smoke_contract.py` (`dry_run|intent_chain`).
* Добавлены regression tests:
  * `tests/test_check_automation_smoke_contract.py`
* CI workflow обновлен:
  * после smoke-run для `automation_dry_run` выполняется contract-check
  * после smoke-run для `automation_intent_chain_smoke` выполняется contract-check
* Docs sync:
  * `docs/RUNBOOK.md` (M6.6 section + CI smoke commands)
  * `docs/DECISIONS.md`, `PLANS.md`, `TODO.md`
* Финальный regression-check:
  * команда: `python -m pytest`
  * результат: `151 passed`
* Локальная smoke-валидация новых CI contracts:
  * `check_automation_smoke_contract --mode dry_run` -> `status=ok`
  * `check_automation_smoke_contract --mode intent_chain` -> `status=ok`

## Update (same date, M5.3 nightly guardrail workflow)

* Добавлен workflow:
  `.github/workflows/kag-neo4j-guardrail-nightly.yml`.
* Nightly chain:
  * `kag_build_baseline` на `tests/fixtures/kag_neo4j_docs_sample.jsonl`
  * `kag_sync_neo4j` в Neo4j service
  * `eval_kag_neo4j` для `sample` + `hard` (`--warmup-runs 1`)
  * `check_kag_neo4j_guardrail.py` для обоих профилей
* Для nightly path добавлен fixture corpus:
  * `tests/fixtures/kag_neo4j_docs_sample.jsonl`
* Runbook обновлен разделом `M5.3`.

## Update (same date, M6.7 automation CI summary/report)

* `scripts/check_automation_smoke_contract.py` расширен опцией `--summary-json`.
* Summary payload содержит:
  * `mode`, `status`, `ok`, `run_dir`
  * `thresholds`, `observed`, `violations`, `error`
* CI workflow `pytest` обновлен:
  * contract-check steps теперь пишут summary JSON
  * добавлен step summary table в `GITHUB_STEP_SUMMARY`
  * добавлен upload artifact `automation-smoke-contract-summaries`
* Добавлены/обновлены tests:
  * `tests/test_check_automation_smoke_contract.py` (включая summary-json path)
* Финальный regression-check:
  * команда: `python -m pytest`
  * результат: `152 passed`
* Локальная проверка summary-json path:
  * `check_automation_smoke_contract --mode dry_run --summary-json ...` -> `status=ok`
  * `check_automation_smoke_contract --mode intent_chain --summary-json ...` -> `status=ok`

## Update (same date, M5.4 guardrail trend snapshot)

* Добавлен script:
  `scripts/kag_guardrail_trend_snapshot.py`.
* Script читает latest/history окна по `sample` и `hard` nightly eval runs и пишет:
  * `trend_snapshot.json`
  * `summary.md`
* Добавлены tests:
  * `tests/test_kag_guardrail_trend_snapshot.py` (`success`, `missing profile`, `CLI --help`)
* Runbook/Decisions/TODO/Plans синхронизированы под новый trend-step.

## Update (same date, M6.8 troubleshooting playbook)

* В `docs/RUNBOOK.md` добавлен раздел:
  `M6.8: Troubleshooting automation smoke contract failures (CI)`.
* Зафиксирован короткий диагностический чек-лист:
  * где смотреть `contract_summary.json`
  * как интерпретировать `violations`/`error`
  * какие команды воспроизводить локально
  * как проверять `intent_type` и `runs-dir` consistency.

## Update (same date, M5.5 nightly trend summary integration)

* Обновлен workflow:
  `.github/workflows/kag-neo4j-guardrail-nightly.yml`.
* В nightly chain добавлены шаги:
  * `Build guardrail trend snapshot` (`scripts/kag_guardrail_trend_snapshot.py`)
  * `Resolve trend snapshot paths`
  * `Summary - KAG guardrail trend` (короткий report в `GITHUB_STEP_SUMMARY`)
* Upload artifact расширен:
  * добавлен `runs/nightly-kag-trend`
* Runbook/Decisions/TODO/Plans синхронизированы под новый `M5.5` path.

## Update (same date, M6.9 + M5.6)

* `M6.9`:
  * В `.github/workflows/pytest.yml` (`Summary - Automation smoke contracts`) добавлен quick-link на
    `docs/RUNBOOK.md#m68-troubleshooting-automation-smoke-contract-failures-ci`.
  * Цель: сократить time-to-diagnosis при падениях `check_automation_smoke_contract`.
* `M5.6`:
  * `scripts/kag_guardrail_trend_snapshot.py` расширен `rolling_baseline` логикой:
    * новый параметр `--baseline-window` (default `3`)
    * snapshot теперь пишет baseline-метрики и `latest-baseline` дельты по `sample/hard`
      в `trend_snapshot.json` и `summary.md`
  * Nightly workflow `.github/workflows/kag-neo4j-guardrail-nightly.yml` обновлен:
    * `Build guardrail trend snapshot` использует `--baseline-window 5`
    * step summary выводит baseline counts и `latest-baseline` delta по `mrr`
  * `docs/RUNBOOK.md` синхронизирован под новый CLI/artifact контракт.
* Tests:
  * обновлены `tests/test_kag_guardrail_trend_snapshot.py` (добавлен кейс с пустым baseline при single-run истории)
  * полный regression-check: `python -m pytest` -> `156 passed`.

## Update (same date, M6.10 + M5.7)

* `M6.10`:
  * В nightly workflow `.github/workflows/kag-neo4j-guardrail-nightly.yml` (`Summary - KAG guardrail trend`)
    добавлен quick-link на `docs/RUNBOOK.md#m68-troubleshooting-automation-smoke-contract-failures-ci`.
* `M5.7`:
  * `scripts/kag_guardrail_trend_snapshot.py` расширен regression-флагами для rolling-baseline:
    * `mrr_status`: `improved|stable|regressed|insufficient_history`
    * `latency_p95_status`: `improved|stable|regressed|insufficient_history`
    * `has_any_regression` + machine-readable признаки по mrr/latency.
  * `summary.md` trend snapshot теперь выводит regression-статусы по `sample/hard`.
  * Nightly step summary теперь выводит regression-статусы и `has_any_regression` для обоих профилей.
* Tests:
  * обновлены `tests/test_kag_guardrail_trend_snapshot.py` под regression-flags контракт.
  * полный regression-check: `python -m pytest` -> `156 passed`.

## Update (same date, session-close doc sync)

* `README.md` приведен к lightweight status-формату:
  * короткий актуальный snapshot
  * ссылки на каноничные документы вместо длинного дублирующего status-блока.
* `MANIFEST.md` обновлен до краткого snapshot-формата:
  * актуальная дата/статус тестов
  * active capabilities
  * canonical docs map.
* Добавлен weekly review шаблон:
  * `docs/SESSION_WEEKLY_TEMPLATE.md` (one-screen формат: улучшения/блокеры/следующие шаги).
* `docs/SOURCE_OF_TRUTH.md` синхронизирован:
  * добавлены роли `README.md`, `MANIFEST.md` и weekly template.
* `TODO.md` синхронизирован по закрытию doc-задач:
  * weekly template и README simplification переведены в done.
* Финальный session-check:
  * `python -m pytest` -> `156 passed`.
