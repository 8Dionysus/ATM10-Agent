# SESSION_2026-02-23

Brief summary of the session dated **2026-02-23**.

## What has been done

* An audit of the project's current Markdown documents was completed:
  * `README.md`, `MANIFEST.md`, `TODO.md`, `PLANS.md`
  * `docs/RUNBOOK.md`, `docs/DECISIONS.md`, `docs/SOURCE_OF_TRUTH.md`
  * `docs/ARCHIVED_TRACKS.md`, `docs/QWEN3_MODEL_STACK.md`
  * latest snapshots: `docs/SESSION_2026-02-20.md`, `docs/SESSION_2026-02-22.md`
* Actual baseline tests confirmed:
  * command: `python -m pytest`
  * result: `136 passed`
* Synchronized by `TODO.md`:
  * status snapshot updated to `2026-02-23`
  * added `Session Focus (2026-02-23)` with current session priorities

## Current session plan

1. M6.2: define the next practical step after `automation_dry_run` without moving to real input events.
2. KAG guardrail: fix canonical benchmark profile (`sample + hard`, `--warmup-runs 1`) and agree on quality/latency thresholds.
3. CI hygiene: select 1-2 runnable smoke scripts to expand CI without flaky regressions.
4. After changes in docs/scripts, execute `python -m pytest` and record the result in the session log.

## Limitations and boundaries (confirmed)

* WIP limit: maximum 3 active tasks simultaneously.
* Active voice path: `whisper_genai`; `qwen_asr`/`Qwen3-TTS` remain archived/recoverable.
* The document source-of-truth policy is observed:
  * execution plan in `TODO.md`
  * goals/DoD in `PLANS.md`
  * solutions in `docs/DECISIONS.md`
  * commands in `docs/RUNBOOK.md`

## Update (same date, M6.2 action-plan contract)

* The `automation_plan_v1` contract in `scripts/automation_dry_run.py` has been clarified:
  * added explicit validation `schema_version`
  * added normalization of block `intent` (`goal/priority/tags/constraints`)
  * added uniqueness check `action.id`
* Added canonical demo scenarios:
  * `tests/fixtures/automation_plan_quest_book.json`
  * `tests/fixtures/automation_plan_inventory_check.json`
* Added contract regression tests:
  * positive fixture-path for `automation_plan_v1 + intent`
  * negative case on unsupported `schema_version`
* Updated docs:
  * `docs/RUNBOOK.md` (M6 sample + fixture commands)
  * `docs/DECISIONS.md` (M6.2 contract solution)
  * `TODO.md` (`M6.2` moved to done, new next-step `M6.3`)
* Final regression check:
  * command: `python -m pytest`
  * result: `138 passed`

## Update (same date, M6.3 intent adapter)

* Added new dry-run adapter:
  `scripts/intent_to_automation_plan.py`.
* Adapter contract:
  * input: `automation_intent_v1`
  * output: `automation_plan_v1`
  * supported template-intents: `open_quest_book`, `check_inventory_tool`
* Artifact contract:
  * `runs/<timestamp>-intent-to-automation-plan/run.json`
  * `runs/<timestamp>-intent-to-automation-plan/automation_plan.json`
* Added tests/fixtures:
  * `tests/fixtures/intent_open_quest_book.json`
* Added regression tests:
  * `tests/test_intent_to_automation_plan.py` (`success`, `invalid intent_type`, `CLI --help`)
* Updated docs:
  * `docs/RUNBOOK.md` (M6.3 section + e2e chain command)
  * `docs/DECISIONS.md` (M6.3 decision log)
  * `TODO.md` (`M6.3` is marked as done, `M6.4` is moved to Now)
* Final regression check:
  * command: `python -m pytest`
  * result: `141 passed`

## Update (same date, M6.4 unified smoke chain)

* Added a single dry-run smoke entrypoint:
  `scripts/automation_intent_chain_smoke.py`.
* Execution chain:
  * `automation_intent_v1` -> `automation_plan_v1` (adapter)
  * `automation_plan_v1` -> `execution_plan` (`automation_dry_run`)
* Artifact contract:
  * `runs/<timestamp>-automation-intent-chain-smoke/run.json`
  * `runs/<timestamp>-automation-intent-chain-smoke/chain_summary.json`
  * `runs/<timestamp>-automation-intent-chain-smoke/automation_plan.json`
  * `runs/<timestamp>-automation-intent-chain-smoke/child_runs/*`
* Added regression tests:
  * `tests/test_automation_intent_chain_smoke.py` (`success`, `invalid intent`, `CLI --help`)
* Updated docs:
  * `docs/RUNBOOK.md` (M6.4 section)
  * `docs/DECISIONS.md` (M6.4 decision log)
  * `TODO.md` (`M6.4` is marked as done, `M6.5` is moved to Now)

## Update (same date, M6.5 CI smoke expansion)

* Updated CI workflow:
  * `.github/workflows/pytest.yml` (job `smoke`)
* Added 2 new lightweight smoke-steps:
  * `python scripts/automation_dry_run.py --plan-json tests/fixtures/automation_plan_quest_book.json --runs-dir runs/ci-smoke-automation-dry-run`
  * `python scripts/automation_intent_chain_smoke.py --intent-json tests/fixtures/intent_open_quest_book.json --runs-dir runs/ci-smoke-automation-chain`
* Selected approach for flaky-control:
  * fixture-only scripts (no Docker/audio/external services)
  * deterministic dry-run path with artifact contract
* Synchronized docs:
  * `docs/RUNBOOK.md` (CI smoke section)
  * `docs/DECISIONS.md` (M6.5 decision log)
  * `PLANS.md` (G3 open task updated to stability/thresholds)
  * `TODO.md` (`M6.5` + CI hygiene are marked as done)
* Local check of new CI smoke commands:
  * `python scripts/automation_dry_run.py --plan-json tests/fixtures/automation_plan_quest_book.json --runs-dir runs/ci-smoke-automation-dry-run-local`
  * `python scripts/automation_intent_chain_smoke.py --intent-json tests/fixtures/intent_open_quest_book.json --runs-dir runs/ci-smoke-automation-chain-local`
  * both commands completed successfully and recorded the expected artifacts.

## Update (same date, KAG guardrail profile)

* Added guardrail checker:
  `scripts/check_kag_neo4j_guardrail.py`.
* Canonical threshold profiles recorded:
  * `sample`: `recall>=1.0`, `mrr>=0.80`, `hit-rate>=1.0`, `latency_p95<=120ms`
  * `hard`: `recall>=1.0`, `mrr>=0.90`, `hit-rate>=1.0`, `latency_p95<=130ms`
* Added regression tests:
  * `tests/test_check_kag_neo4j_guardrail.py`
* Runbook updated with guardrail check commands on `eval_results.json`.

## Update (same date, M6.6 automation smoke acceptance thresholds)

* Added CI contract checker:
  `scripts/check_automation_smoke_contract.py` (`dry_run|intent_chain`).
* Added regression tests:
  * `tests/test_check_automation_smoke_contract.py`
* CI workflow updated:
  * after smoke-run for `automation_dry_run` contract-check is performed
  * after smoke-run for `automation_intent_chain_smoke` contract-check is performed
* Docs sync:
  * `docs/RUNBOOK.md` (M6.6 section + CI smoke commands)
  * `docs/DECISIONS.md`, `PLANS.md`, `TODO.md`
* Final regression check:
  * command: `python -m pytest`
  * result: `151 passed`
* Local smoke validation of new CI contracts:
  * `check_automation_smoke_contract --mode dry_run` -> `status=ok`
  * `check_automation_smoke_contract --mode intent_chain` -> `status=ok`

## Update (same date, M5.3 nightly guardrail workflow)

* Added workflow:
  `.github/workflows/kag-neo4j-guardrail-nightly.yml`.
* Nightly chain:
  * `kag_build_baseline` to `tests/fixtures/kag_neo4j_docs_sample.jsonl`
  * `kag_sync_neo4j` in Neo4j service
  * `eval_kag_neo4j` for `sample` + `hard` (`--warmup-runs 1`)
  * `check_kag_neo4j_guardrail.py` for both profiles
* Added fixture corpus for nightly path:
  * `tests/fixtures/kag_neo4j_docs_sample.jsonl`
* The runbook has been updated with section `M5.3`.

## Update (same date, M6.7 automation CI summary/report)

* `scripts/check_automation_smoke_contract.py` is extended with option `--summary-json`.
* Summary payload contains:
  * `mode`, `status`, `ok`, `run_dir`
  * `thresholds`, `observed`, `violations`, `error`
* CI workflow `pytest` updated:
  * contract-check steps now write summary JSON
  * added step summary table to `GITHUB_STEP_SUMMARY`
  * added upload artifact `automation-smoke-contract-summaries`
* Added/updated tests:
  * `tests/test_check_automation_smoke_contract.py` (including summary-json path)
* Final regression check:
  * command: `python -m pytest`
  * result: `152 passed`
* Local check summary-json path:
  * `check_automation_smoke_contract --mode dry_run --summary-json ...` -> `status=ok`
  * `check_automation_smoke_contract --mode intent_chain --summary-json ...` -> `status=ok`

## Update (same date, M5.4 guardrail trend snapshot)

* Added script:
  `scripts/kag_guardrail_trend_snapshot.py`.
* The Script reads the latest/history windows by `sample` and `hard` nightly eval runs and writes:
  * `trend_snapshot.json`
  * `summary.md`
* Added tests:
  * `tests/test_kag_guardrail_trend_snapshot.py` (`success`, `missing profile`, `CLI --help`)
* Runbook/Decisions/TODO/Plans are synchronized to the new trend-step.

## Update (same date, M6.8 troubleshooting playbook)

* Section added to `docs/RUNBOOK.md`:
  `M6.8: Troubleshooting automation smoke contract failures (CI)`.
* A short diagnostic checklist has been recorded:
  * where to watch `contract_summary.json`
  * how to interpret `violations`/`error`
  * what commands to play locally
  * how to check `intent_type` and `runs-dir` consistency.

## Update (same date, M5.5 nightly trend summary integration)

* Updated workflow:
  `.github/workflows/kag-neo4j-guardrail-nightly.yml`.
* Added steps to nightly chain:
  * `Build guardrail trend snapshot` (`scripts/kag_guardrail_trend_snapshot.py`)
  * `Resolve trend snapshot paths`
  * `Summary - KAG guardrail trend` (short report in `GITHUB_STEP_SUMMARY`)
* Upload artifact expanded:
  * added `runs/nightly-kag-trend`
* Runbook/Decisions/TODO/Plans are synchronized to the new `M5.5` path.

## Update (same date, M6.9 + M5.6)

* `M6.9`:
  * Added quick-link to `.github/workflows/pytest.yml` (`Summary - Automation smoke contracts`)
    `docs/RUNBOOK.md#m68-troubleshooting-automation-smoke-contract-failures-ci`.
  * Goal: reduce time-to-diagnosis for `check_automation_smoke_contract` falls.
* `M5.6`:
  * `scripts/kag_guardrail_trend_snapshot.py` is extended by `rolling_baseline` with logic:
    * new parameter `--baseline-window` (default `3`)
    * snapshot now writes baseline metrics and `latest-baseline` deltas by `sample/hard`
      in `trend_snapshot.json` and `summary.md`
  * Nightly workflow `.github/workflows/kag-neo4j-guardrail-nightly.yml` updated:
    * `Build guardrail trend snapshot` uses `--baseline-window 5`
    * step summary displays baseline counts and `latest-baseline` delta by `mrr`
  * `docs/RUNBOOK.md` is synchronized under the new CLI/artifact contract.
* Tests:
  * updated `tests/test_kag_guardrail_trend_snapshot.py` (added a case with an empty baseline for single-run history)
  * full regression-check: `python -m pytest` -> `156 passed`.

## Update (same date, M6.10 + M5.7)

* `M6.10`:
  * In nightly workflow `.github/workflows/kag-neo4j-guardrail-nightly.yml` (`Summary - KAG guardrail trend`)
    added quick-link to `docs/RUNBOOK.md#m68-troubleshooting-automation-smoke-contract-failures-ci`.
* `M5.7`:
  * `scripts/kag_guardrail_trend_snapshot.py` extended with regression flags for rolling-baseline:
    * `mrr_status`: `improved|stable|regressed|insufficient_history`
    * `latency_p95_status`: `improved|stable|regressed|insufficient_history`
    * `has_any_regression` + machine-readable signs by mrr/latency.
  * `summary.md` trend snapshot now displays regression statuses for `sample/hard`.
  * Nightly step summary now displays regression statuses and `has_any_regression` for both profiles.
* Tests:
  * updated `tests/test_kag_guardrail_trend_snapshot.py` under regression-flags contract.
  * full regression-check: `python -m pytest` -> `156 passed`.

## Update (same date, session-close doc sync)

* `README.md` is converted to a lightweight status format:
  * short current snapshot
  * links to canonical documents instead of a long duplicate status block.
* `MANIFEST.md` updated to short snapshot format:
  * current date/status of tests
  * active capabilities
  * canonical docs map.
* Added weekly review template:
  * `docs/SESSION_WEEKLY_TEMPLATE.md` (one-screen format: improvements/blockers/next steps).
* `docs/SOURCE_OF_TRUTH.md` synchronized:
  * added roles `README.md`, `MANIFEST.md` and weekly template.
* `TODO.md` is synchronized to close doc tasks:
  * weekly template and README simplification are translated into done.
* Final session-check:
  * `python -m pytest` -> `156 passed`.
