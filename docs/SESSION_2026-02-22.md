# SESSION_2026-02-22

Brief summary of the session dated **2026-02-22**.

## What has been done

* Checked the baseline of the repository: `git status` is clean.
* Completed `python -m pytest`: result `119 passed`.
* Voice support probe launched:
  * command: `python scripts/probe_qwen3_voice_support.py`
  * artifact: `runs/20260222_141416-qwen3-voice-probe/`
  * result: `qwen3_asr`/`qwen3_tts*` -> `status=import_error` (`No module named 'transformers'`).
* Dry-run ASR custom export completed:
  * command: `python -m scripts.export_qwen3_custom_openvino --preset qwen3-asr-0.6b`
  * artifact: `runs/20260222_141431-qwen3-custom-export/`
  * result: `unlock_gate.ready=false`, `observed_status=import_error`.
* Run again in `.venv` (export-toolchain path):
  * command: `.\.venv\Scripts\python.exe scripts/probe_qwen3_voice_support.py`
  * artifact: `runs/20260222_142450-qwen3-voice-probe/`
  * result: `qwen3_asr` -> `status=blocked_upstream`.
  * command: `.\.venv\Scripts\python.exe -m scripts.export_qwen3_custom_openvino --preset qwen3-asr-0.6b --execute`
  * artifact: `runs/20260222_142518-qwen3-custom-export/`
  * result: `status=blocked`, `error_code=unlock_gate_blocked`, `observed_status=blocked_upstream`.
* Fixed CLI compatibility `scripts/export_qwen3_custom_openvino.py`:
  * the script now works as both `python scripts/...` and `python -m scripts...`.
  * checking script-mode: `python scripts/export_qwen3_custom_openvino.py --preset qwen3-asr-0.6b`
    (artifact: `runs/20260222_141812-qwen3-custom-export/`).
  * added regression test: `test_custom_export_cli_help_works_when_run_as_script`.
* Added alternative ASR-path to OpenVINO GenAI + Whisper v3 Turbo:
  * new entrypoint: `scripts/asr_demo_whisper_genai.py` (`--device CPU|GPU|NPU`, timestamps options)
  * tests: `tests/test_asr_demo_whisper_genai.py`
  * runbook updated with section for Whisper v3 Turbo conversion/run.
* An end-to-end run of Whisper v3 Turbo was completed on the NPU:
  * runtime installed: `python -m pip install \"openvino-genai>=2025.4.0\"`
  * export model: `optimum-cli export openvino --model openai/whisper-large-v3-turbo models\\whisper-large-v3-turbo-ov`
  * successful demo run: `python scripts/asr_demo_whisper_genai.py --model-dir models\\whisper-large-v3-turbo-ov --audio-in runs\\20260222_whisper_smoke_input\\tmp\\tone.wav --device NPU`
  * artifact run: `runs/20260222_150612-asr-whisper-genai/` (`run.json` status=`ok`, `transcription.json` created).
* Added backend-switch to long-lived voice runtime service:
  * `scripts/voice_runtime_service.py` now supports `--asr-backend qwen_asr|whisper_genai`
  * for `whisper_genai` parameters `--asr-device`, `--asr-task`, `--asr-language` have been added.
* Performed e2e service run with `whisper_genai` on NPU and real microphone:
  * service run: `runs/20260222_151556-voice-service/` (`asr_backend=whisper_genai`, `asr_device=NPU`)
  * health client run: `runs/20260222_151603-voice-client/`
  * ASR client run (mic 3s): `runs/20260222_151611-voice-client/`
    (`input_device_name=\"Perenaznachenie zvukovykh ustr. - Input\"`, `asr_response.json` contains recognized text).
* Added and executed backend benchmark on one `.wav` set:
  * script: `scripts/benchmark_asr_backends.py`
  * run: `runs/20260222_152347-asr-backend-bench/`
  * summary: `qwen_asr` load `14.047s`, avg latency `1.377s`; `whisper_genai` load `14.418s`, avg latency `1.364s` (both `6/6` ok).
* Added startup warmup request to long-lived service:
  * new parameters: `--asr-warmup-request`, `--asr-warmup-audio`, `--asr-warmup-language`
  * helper launcher: `scripts/start_voice_whisper_npu.ps1`
  * runtime validation run: `runs/20260222_153958-voice-service/`
    (`preload.asr.warmup.ok=true`, `latency_sec=1.4144`,
    `requested_language=\"en\"`, warmup audio saved in `tmp/asr_warmup_silence.wav`).
* Warm-path benchmark (10 repeats of one microphone WAV):
  * run `runs/20260222_152914-asr-backend-bench/`: `qwen_asr` avg/p95 `1.318/2.317s`, `whisper_genai` avg/p95 `1.460/1.560s`
  * run `runs/20260222_153034-asr-backend-bench/` (`qwen_max_new_tokens=128`): `qwen_asr` avg/p95 `1.317/2.216s`, `whisper_genai` avg/p95 `1.485/1.654s`.
* The status documents are synchronized to match the actual baseline:
  * `README.md`, `PLANS.md`, `TODO.md`, `MANIFEST.md`, `docs/RUNBOOK.md`, `docs/DECISIONS.md`.

## Key Takeaway

OpenVINO self-conversion for `Qwen3-ASR-0.6B` remains open gap.
After a separate run in `.venv` with export toolchain, the blocker was confirmed as `blocked_upstream`:
`transformers` does not recognize the `qwen3_asr` architecture, so `unlock_gate.ready=false` even with toolchain installed.

## Update (same date, archive policy)

`Qwen3-ASR-0.6B` has been moved to archived/recoverable status:

* active ASR path = `whisper_genai` (`OpenVINO GenAI + Whisper v3 Turbo`);
* `qwen_asr` is saved in the code without removal and only explicit opt-in is enabled:
  * `scripts/voice_runtime_service.py --asr-backend qwen_asr --allow-archived-qwen-asr ...`
  * `scripts/asr_demo.py --allow-archived-qwen-asr ...`
  * `scripts/benchmark_asr_backends.py --include-archived-qwen-asr ...`

## Update (same date, M3.1 progression)

* Added runnable text-core entrypoint:
  `scripts/text_core_openvino_demo.py` (`OpenVINO GenAI`, `prompt -> response.json`).
* Added profile-layer for retrieval:
  `src/rag/retrieval_profiles.py` with profile `baseline|ov_production`.
* `scripts/retrieve_demo.py` and `scripts/eval_retrieval.py` now support `--profile`
  and manual overrides over profile.

## Update (same date, HUD OCR baseline)

* Added dependency-light OCR baseline for HUD:
  `scripts/hud_ocr_baseline.py` (Tesseract CLI wrapper).
* Artifact contract: `runs/<timestamp>-hud-ocr/{run.json,ocr.json,ocr.txt}`.
* Added tests: `tests/test_hud_ocr_baseline.py` (`success`, `missing runtime`, `CLI --help`).

## Update (same date, HUD mod-hook baseline)

* Added `scripts/hud_mod_hook_baseline.py` (hook JSON ingest -> normalized HUD artifacts).
* Artifact contract: `runs/<timestamp>-hud-hook/{run.json,hook_raw.json,hook_normalized.json,hud_text.txt}`.
* Added tests: `tests/test_hud_mod_hook_baseline.py` (`success`, `invalid payload`, `CLI --help`).

## Update (same date, KAG baseline)

* Added file-based KAG baseline:
  * `src/kag/baseline.py` (`build_kag_graph`, `query_kag_graph`)
  * `scripts/kag_build_baseline.py` (docs -> `kag_graph.json`)
  * `scripts/kag_query_demo.py` (`kag_graph.json` + query -> `kag_query_results.json`)
* Artifact contract:
  * `runs/<timestamp>-kag-build/{run.json,kag_graph.json}`
  * `runs/<timestamp>-kag-query/{run.json,kag_query_results.json}`
* Tests added:
  * `tests/test_kag_build_baseline.py` (`build success`, `CLI --help`)
  * `tests/test_kag_query_demo.py` (`query success`, `CLI --help`)
* After KAG integration, `python -m pytest` was run again: `119 passed`.

## Update (same date, KAG Neo4j progression)

* Confirmed KAG's transition to Neo4j as a target stage (without gate by value-check).
* Added backend `src/kag/neo4j_backend.py`:
  * sync `kag_graph.json` -> Neo4j (`Doc`, `Entity`, `MENTIONS`, `COOCCURS`)
  * query from Neo4j (`top-k` + citations, entity-token scoring)
* Added entrypoints:
  * `scripts/kag_sync_neo4j.py`
  * `scripts/kag_query_neo4j.py`
* Tests added:
  * `tests/test_kag_neo4j_backend.py`
  * `tests/test_kag_sync_neo4j.py`
  * `tests/test_kag_query_neo4j.py`
* Updated runbook with `M5.1: KAG via Neo4j` section.
* After Neo4j path integration, complete `python -m pytest`: `127 passed` is executed.

## Update (same date, KAG Neo4j e2e run)

* Raised local Neo4j container:
  `docker run -d --name atm10-neo4j -p 7474:7474 -p 7687:7687 -e NEO4J_AUTH=neo4j/neo4jpass neo4j:5`.
* Full e2e pipeline completed:
  * build: `runs/20260222_164928-kag-build/`
  * sync: `runs/20260222_164942-kag-sync-neo4j/`
  * query: `runs/20260222_165026-kag-query-neo4j/`
* Latency snapshot recorded:
  `runs/20260222_165100-kag-neo4j-e2e/e2e_latency_snapshot.json`.
* Observations on latency (wall-clock):
  * build: `2.036s`
  * sync: `28.373s`
  * query(`steel tools`): `0.619s` (`count=0`)
  * query(`quest`): `0.287s` (`count=5`)

## Update (same date, KAG Neo4j benchmark)

* Added benchmark script:
  `scripts/eval_kag_neo4j.py`.
* Added eval fixture:
  `tests/fixtures/kag_neo4j_eval_sample.jsonl` (7 query-cases).
* Tests added:
  `tests/test_eval_kag_neo4j.py`.
* Baseline benchmark run completed:
  `runs/20260222_165514-kag-neo4j-eval/`.
* Metrics (`topk=5`, `query_count=7`):
  * `mean_recall_at_k=1.0000`
  * `mean_mrr_at_k=0.8571`
  * `hit_rate_at_k=1.0000`
  * `latency_mean_ms=40.05`
  * `latency_p95_ms=53.68`
  * `latency_max_ms=55.87`
* After adding the benchmark script and tests: `python -m pytest` -> `127 passed`.

## Update (same date, KAG Neo4j hard benchmark + summary)

* `scripts/eval_kag_neo4j.py` updated: now writes `summary.md` next to `eval_results.json`.
* Repeated sample-run:
  `runs/20260222_170000-kag-neo4j-eval/`
  (`recall@5=1.0000`, `mrr@5=0.8571`, `hit-rate@5=1.0000`, `latency_p95_ms=63.60`).
* Added hard-cases fixture:
  `tests/fixtures/kag_neo4j_eval_hard.jsonl`.
* Hard-run completed:
  `runs/20260222_170006-kag-neo4j-eval/`
  (`recall@5=0.5000`, `mrr@5=0.3750`, `hit-rate@5=0.5000`, `latency_p95_ms=70.50`).
* Fixed gap-cases for the following uplfit:
  `star`, `pylons`, `undergarden`, `hostile neural networks` (first-hit is not in top-5).

## Update (same date, KAG Neo4j hard-cases uplift)

* `query_kag_neo4j` has been updated in `src/kag/neo4j_backend.py`:
  added lexical fallback by `Doc.title/doc_id` + lexical score boost.
* Repeated hard benchmark:
  `runs/20260222_170453-kag-neo4j-eval/`.
* Uplift result (`topk=5`, `query_count=8`):
  * to: `hit-rate@5=0.5000`, `mrr@5=0.3750` (`runs/20260222_170006-kag-neo4j-eval/`)
  * after: `hit-rate@5=1.0000`, `mrr@5=0.7812` (`runs/20260222_170453-kag-neo4j-eval/`)
* Latency trade-off after uplift:
  `latency_p95_ms` grew to `133.00` (baseline hard-run: `70.50`).

## Update (same date, KAG Neo4j latency tuning)

* `query_kag_neo4j` is further optimized:
  * lexical path: `fulltext -> limited scan fallback` (scan only if fulltext is empty),
  * lexical gating: executed only with `direct_rows < topk`,
  * expansion is disabled for single-token query.
* New hard benchmark run:
  `runs/20260222_171240-kag-neo4j-eval/`.
* Tuning result (`topk=5`, `query_count=8`):
  * `mean_recall_at_k=1.0000`
  * `mean_mrr_at_k=0.8438`
  * `hit_rate_at_k=1.0000`
  * `latency_p95_ms=66.14` (improvement against post-uplift `133.00`).

## Update (same date, KAG Neo4j rank uplift for `star`)

* Added post-merge lexical alignment bonus for single-token query to `src/kag/neo4j_backend.py`:
  * `+0.35` with lexical match query token in `doc_id/title`,
  * add. `+0.05` for canonical chapter path (`:chapters/chapter_`).
* Added regression test:
  `tests/test_kag_neo4j_backend.py::test_query_kag_neo4j_single_token_prefers_chapter_filename_match`.
* Hard benchmark run completed:
  `runs/20260222_213235-kag-neo4j-eval/`.
* Result:
  * `star.first_hit_rank: 4 -> 1`
  * `mean_mrr_at_k: 0.8438 -> 0.9375`
  * `hit_rate_at_k=1.0000`, `latency_p95_ms=96.84`.

## Update (same date, KAG Neo4j latency retuning after `star` uplift)

* Added scan-fallback gating to `query_kag_neo4j`:
  * multi-token: fallback scan is executed only with `direct_rows=0`,
  * single-token: fallback scan is performed only if direct hits do not have lexical alignment according to `title/doc_id`.
* In lexical matching for `doc_id`, the namespace-prefix (`ftbquests:`) is excluded to avoid false bonuses for generic-token requests (for example, `quest`).
* Added regression tests:
  * `test_query_kag_neo4j_skips_scan_fallback_for_multi_token_with_direct_hits`
  * `test_query_kag_neo4j_skips_scan_fallback_for_single_token_with_aligned_direct_hit`
* Eval runs:
  * sample: `runs/20260222_214352-kag-neo4j-eval_01/`
    (`recall@5=1.0000`, `mrr@5=0.8333`, `hit-rate@5=1.0000`, `latency_p95_ms=94.89`)
  * hard: `runs/20260222_214352-kag-neo4j-eval/`
    (`recall@5=1.0000`, `mrr@5=0.9375`, `hit-rate@5=1.0000`, `latency_p95_ms=83.68`).

## Update (same date, KAG Neo4j eval warmup path)

* Added CLI/runner parameter `--warmup-runs` to `scripts/eval_kag_neo4j.py`:
  * warmup performs `N` warmup passes against eval queries,
  * warmup requests are not taken into account in `eval_results.json` metrics,
  * warmup information is written to `run.json` (`params.warmup_runs`, `warmup.executed_calls`).
* Test added:
  `tests/test_eval_kag_neo4j.py::test_run_eval_kag_neo4j_warmup_runs_do_not_affect_metrics`.
* Sequential runs with `--warmup-runs 1`:
  * sample: `runs/20260222_215053-kag-neo4j-eval/`
    (`recall@5=1.0000`, `mrr@5=0.8333`, `hit-rate@5=1.0000`, `latency_p95_ms=72.10`)
  * hard: `runs/20260222_215058-kag-neo4j-eval/`
    (`recall@5=1.0000`, `mrr@5=0.9375`, `hit-rate@5=1.0000`, `latency_p95_ms=92.41`, `star.first_hit_rank=1`).
* A/B stability check (hard set, 3x runs):
  * without warmup (`--warmup-runs 0`): `avg_p95=85.98`, `min=75.22`, `max=102.59`
  * with warmup (`--warmup-runs 1`): `avg_p95=76.25`, `min=67.51`, `max=88.55`
  * quality stable in both groups: `mrr@5=0.9375`.
* Added mini-benchmark script:
  `scripts/compare_kag_neo4j_warmup.py` (baseline/candidate warmup profiles + aggregate delta report).
* Smoke run:
  `runs/20260222_215707-kag-neo4j-warmup-compare/`
  (`p95_delta_ms=-28.57`, `p95_improvement_ms=28.57`, `p95_improvement_pct=33.92`).
* Runbook updated:
  nightly hard benchmark profile attached to `--warmup-runs 1`,
  added warmup A/B compare command.

## Update (same date, Automation dry-run scaffold)

* Added secure entrypoint:
  `scripts/automation_dry_run.py` (`--plan-json`).
* Contract: script only validates/normalizes action-plan and writes artifacts;
  real keyboard/mouse events are not executed (`dry_run=true`).
* Artifacts:
  `runs/<timestamp>-automation-dry-run/{run.json,actions_normalized.json,execution_plan.json}`.
* Tests added:
  `tests/test_automation_dry_run.py` (`success`, `invalid payload`, `CLI --help`).
* Smoke run:
  `runs/20260222_220630-automation-dry-run/`
  (`action_count=3`, `step_count=4`, `estimated_total_duration_ms=590`).
* Runbook/Plans/TODO are synchronized for the new dry-run automation path.

## Session close snapshot (2026-02-22)

* Canonical sample benchmark run:
  `runs/20260222_171620-kag-neo4j-eval/`
  (`recall@5=1.0000`, `mrr@5=0.8571`, `hit-rate@5=1.0000`, `latency_p95_ms=70.96`).
* Canonical hard-cases benchmark run (post-latency-tuning):
  `runs/20260222_171240-kag-neo4j-eval/`
  (`recall@5=1.0000`, `mrr@5=0.8438`, `hit-rate@5=1.0000`, `latency_p95_ms=66.14`).
* Regression ladder (for context):
  `runs/20260222_170006-kag-neo4j-eval/` -> `runs/20260222_170453-kag-neo4j-eval/` -> `runs/20260222_171240-kag-neo4j-eval/`.

## Update (same date, docs governance refactor)

* A quick audit of `TODO.md` and `PLANS.md` was completed, the structure was unified through `docs/SOURCE_OF_TRUTH.md`:
  * `TODO.md` — execution-only (Now/Next/Blocked/Done this week, WIP-limit=3)
  * `PLANS.md` — goals/milestones/DoD/risks
  * archived/recoverable directions moved to `docs/ARCHIVED_TRACKS.md`
* After the documentation refactor, a full regression check was performed:
  `python -m pytest` -> `136 passed`.
