# SESSION_2026-02-22

Краткий конспект сессии от **2026-02-22**.

## Что сделано

* Проверен baseline репозитория: `git status` чистый.
* Выполнен `python -m pytest`: итог `119 passed`.
* Запущен voice support probe:
  * команда: `python scripts/probe_qwen3_voice_support.py`
  * artifact: `runs/20260222_141416-qwen3-voice-probe/`
  * результат: `qwen3_asr`/`qwen3_tts*` -> `status=import_error` (`No module named 'transformers'`).
* Выполнен dry-run ASR custom export:
  * команда: `python -m scripts.export_qwen3_custom_openvino --preset qwen3-asr-0.6b`
  * artifact: `runs/20260222_141431-qwen3-custom-export/`
  * результат: `unlock_gate.ready=false`, `observed_status=import_error`.
* Выполнен повторный прогон в `.venv` (export-toolchain path):
  * команда: `.\.venv\Scripts\python.exe scripts/probe_qwen3_voice_support.py`
  * artifact: `runs/20260222_142450-qwen3-voice-probe/`
  * результат: `qwen3_asr` -> `status=blocked_upstream`.
  * команда: `.\.venv\Scripts\python.exe -m scripts.export_qwen3_custom_openvino --preset qwen3-asr-0.6b --execute`
  * artifact: `runs/20260222_142518-qwen3-custom-export/`
  * результат: `status=blocked`, `error_code=unlock_gate_blocked`, `observed_status=blocked_upstream`.
* Исправлена CLI-совместимость `scripts/export_qwen3_custom_openvino.py`:
  * скрипт теперь работает и как `python scripts/...`, и как `python -m scripts...`.
  * проверка script-mode: `python scripts/export_qwen3_custom_openvino.py --preset qwen3-asr-0.6b`
    (artifact: `runs/20260222_141812-qwen3-custom-export/`).
  * добавлен regression test: `test_custom_export_cli_help_works_when_run_as_script`.
* Добавлен альтернативный ASR-path на OpenVINO GenAI + Whisper v3 Turbo:
  * новый entrypoint: `scripts/asr_demo_whisper_genai.py` (`--device CPU|GPU|NPU`, timestamps options)
  * тесты: `tests/test_asr_demo_whisper_genai.py`
  * runbook обновлен разделом для Whisper v3 Turbo conversion/run.
* Выполнен end-to-end прогон Whisper v3 Turbo на NPU:
  * установлен runtime: `python -m pip install \"openvino-genai>=2025.4.0\"`
  * экспорт модели: `optimum-cli export openvino --model openai/whisper-large-v3-turbo models\\whisper-large-v3-turbo-ov`
  * успешный demo run: `python scripts/asr_demo_whisper_genai.py --model-dir models\\whisper-large-v3-turbo-ov --audio-in runs\\20260222_whisper_smoke_input\\tmp\\tone.wav --device NPU`
  * artifact run: `runs/20260222_150612-asr-whisper-genai/` (`run.json` status=`ok`, `transcription.json` создан).
* Добавлен backend-switch в long-lived voice runtime service:
  * `scripts/voice_runtime_service.py` теперь поддерживает `--asr-backend qwen_asr|whisper_genai`
  * для `whisper_genai` добавлены параметры `--asr-device`, `--asr-task`, `--asr-language`.
* Выполнен e2e service run с `whisper_genai` на NPU и реальным микрофоном:
  * service run: `runs/20260222_151556-voice-service/` (`asr_backend=whisper_genai`, `asr_device=NPU`)
  * health client run: `runs/20260222_151603-voice-client/`
  * ASR client run (mic 3s): `runs/20260222_151611-voice-client/`
    (`input_device_name=\"Переназначение звуковых устр. - Input\"`, `asr_response.json` содержит распознанный текст).
* Добавлен и выполнен backend benchmark на одном наборе `.wav`:
  * script: `scripts/benchmark_asr_backends.py`
  * run: `runs/20260222_152347-asr-backend-bench/`
  * summary: `qwen_asr` load `14.047s`, avg latency `1.377s`; `whisper_genai` load `14.418s`, avg latency `1.364s` (оба `6/6` ok).
* Добавлен startup warmup request в long-lived service:
  * новые параметры: `--asr-warmup-request`, `--asr-warmup-audio`, `--asr-warmup-language`
  * helper launcher: `scripts/start_voice_whisper_npu.ps1`
  * runtime validation run: `runs/20260222_153958-voice-service/`
    (`preload.asr.warmup.ok=true`, `latency_sec=1.4144`,
    `requested_language=\"en\"`, warmup audio сохранён в `tmp/asr_warmup_silence.wav`).
* Warm-path benchmark (10 повторов одного микрофонного WAV):
  * run `runs/20260222_152914-asr-backend-bench/`: `qwen_asr` avg/p95 `1.318/2.317s`, `whisper_genai` avg/p95 `1.460/1.560s`
  * run `runs/20260222_153034-asr-backend-bench/` (`qwen_max_new_tokens=128`): `qwen_asr` avg/p95 `1.317/2.216s`, `whisper_genai` avg/p95 `1.485/1.654s`.
* Синхронизированы status-документы под фактический baseline:
  * `README.md`, `PLANS.md`, `TODO.md`, `MANIFEST.md`, `docs/RUNBOOK.md`, `docs/DECISIONS.md`.

## Ключевой вывод

OpenVINO self-conversion для `Qwen3-ASR-0.6B` остаётся open gap.
После отдельного прогона в `.venv` с export toolchain блокер подтверждён как `blocked_upstream`:
`transformers` не распознаёт архитектуру `qwen3_asr`, поэтому `unlock_gate.ready=false` даже при установленном toolchain.

## Update (same date, archive policy)

`Qwen3-ASR-0.6B` переведен в archived/recoverable status:

* active ASR path = `whisper_genai` (`OpenVINO GenAI + Whisper v3 Turbo`);
* `qwen_asr` сохранен в коде без удаления и включается только explicit opt-in:
  * `scripts/voice_runtime_service.py --asr-backend qwen_asr --allow-archived-qwen-asr ...`
  * `scripts/asr_demo.py --allow-archived-qwen-asr ...`
  * `scripts/benchmark_asr_backends.py --include-archived-qwen-asr ...`

## Update (same date, M3.1 progression)

* Добавлен runnable text-core entrypoint:
  `scripts/text_core_openvino_demo.py` (`OpenVINO GenAI`, `prompt -> response.json`).
* Для retrieval добавлен profile-layer:
  `src/rag/retrieval_profiles.py` с profile `baseline|ov_production`.
* `scripts/retrieve_demo.py` и `scripts/eval_retrieval.py` теперь поддерживают `--profile`
  и ручные overrides поверх profile.

## Update (same date, HUD OCR baseline)

* Добавлен dependency-light OCR baseline для HUD:
  `scripts/hud_ocr_baseline.py` (Tesseract CLI wrapper).
* Artifact contract: `runs/<timestamp>-hud-ocr/{run.json,ocr.json,ocr.txt}`.
* Добавлены тесты: `tests/test_hud_ocr_baseline.py` (`success`, `missing runtime`, `CLI --help`).

## Update (same date, HUD mod-hook baseline)

* Добавлен `scripts/hud_mod_hook_baseline.py` (hook JSON ingest -> normalized HUD artifacts).
* Artifact contract: `runs/<timestamp>-hud-hook/{run.json,hook_raw.json,hook_normalized.json,hud_text.txt}`.
* Добавлены тесты: `tests/test_hud_mod_hook_baseline.py` (`success`, `invalid payload`, `CLI --help`).

## Update (same date, KAG baseline)

* Добавлен file-based KAG baseline:
  * `src/kag/baseline.py` (`build_kag_graph`, `query_kag_graph`)
  * `scripts/kag_build_baseline.py` (docs -> `kag_graph.json`)
  * `scripts/kag_query_demo.py` (`kag_graph.json` + query -> `kag_query_results.json`)
* Artifact contract:
  * `runs/<timestamp>-kag-build/{run.json,kag_graph.json}`
  * `runs/<timestamp>-kag-query/{run.json,kag_query_results.json}`
* Добавлены тесты:
  * `tests/test_kag_build_baseline.py` (`build success`, `CLI --help`)
  * `tests/test_kag_query_demo.py` (`query success`, `CLI --help`)
* После интеграции KAG выполнен повторный прогон `python -m pytest`: `119 passed`.

## Update (same date, KAG Neo4j progression)

* Подтвержден переход KAG на Neo4j как целевой этап (без gate по value-check).
* Добавлен backend `src/kag/neo4j_backend.py`:
  * sync `kag_graph.json` -> Neo4j (`Doc`, `Entity`, `MENTIONS`, `COOCCURS`)
  * query из Neo4j (`top-k` + citations, entity-token scoring)
* Добавлены entrypoints:
  * `scripts/kag_sync_neo4j.py`
  * `scripts/kag_query_neo4j.py`
* Добавлены тесты:
  * `tests/test_kag_neo4j_backend.py`
  * `tests/test_kag_sync_neo4j.py`
  * `tests/test_kag_query_neo4j.py`
* Обновлен runbook разделом `M5.1: KAG via Neo4j`.
* После интеграции Neo4j path выполнен полный `python -m pytest`: `127 passed`.

## Update (same date, KAG Neo4j e2e run)

* Поднят локальный Neo4j container:
  `docker run -d --name atm10-neo4j -p 7474:7474 -p 7687:7687 -e NEO4J_AUTH=neo4j/neo4jpass neo4j:5`.
* Выполнен полный e2e pipeline:
  * build: `runs/20260222_164928-kag-build/`
  * sync: `runs/20260222_164942-kag-sync-neo4j/`
  * query: `runs/20260222_165026-kag-query-neo4j/`
* Зафиксирован latency snapshot:
  `runs/20260222_165100-kag-neo4j-e2e/e2e_latency_snapshot.json`.
* Наблюдения по latency (wall-clock):
  * build: `2.036s`
  * sync: `28.373s`
  * query(`steel tools`): `0.619s` (`count=0`)
  * query(`quest`): `0.287s` (`count=5`)

## Update (same date, KAG Neo4j benchmark)

* Добавлен benchmark script:
  `scripts/eval_kag_neo4j.py`.
* Добавлен eval fixture:
  `tests/fixtures/kag_neo4j_eval_sample.jsonl` (7 query-cases).
* Добавлены тесты:
  `tests/test_eval_kag_neo4j.py`.
* Выполнен baseline benchmark run:
  `runs/20260222_165514-kag-neo4j-eval/`.
* Метрики (`topk=5`, `query_count=7`):
  * `mean_recall_at_k=1.0000`
  * `mean_mrr_at_k=0.8571`
  * `hit_rate_at_k=1.0000`
  * `latency_mean_ms=40.05`
  * `latency_p95_ms=53.68`
  * `latency_max_ms=55.87`
* После добавления benchmark script и тестов: `python -m pytest` -> `127 passed`.

## Update (same date, KAG Neo4j hard benchmark + summary)

* `scripts/eval_kag_neo4j.py` обновлен: теперь пишет `summary.md` рядом с `eval_results.json`.
* Выполнен повторный sample-run:
  `runs/20260222_170000-kag-neo4j-eval/`
  (`recall@5=1.0000`, `mrr@5=0.8571`, `hit-rate@5=1.0000`, `latency_p95_ms=63.60`).
* Добавлен hard-cases fixture:
  `tests/fixtures/kag_neo4j_eval_hard.jsonl`.
* Выполнен hard-run:
  `runs/20260222_170006-kag-neo4j-eval/`
  (`recall@5=0.5000`, `mrr@5=0.3750`, `hit-rate@5=0.5000`, `latency_p95_ms=70.50`).
* Зафиксированы gap-cases для следующего uplfit:
  `star`, `pylons`, `undergarden`, `hostile neural networks` (first-hit отсутствует в top-5).

## Update (same date, KAG Neo4j hard-cases uplift)

* В `src/kag/neo4j_backend.py` обновлен `query_kag_neo4j`:
  добавлен lexical fallback по `Doc.title/doc_id` + lexical score boost.
* Повторный hard benchmark:
  `runs/20260222_170453-kag-neo4j-eval/`.
* Результат uplift (`topk=5`, `query_count=8`):
  * до: `hit-rate@5=0.5000`, `mrr@5=0.3750` (`runs/20260222_170006-kag-neo4j-eval/`)
  * после: `hit-rate@5=1.0000`, `mrr@5=0.7812` (`runs/20260222_170453-kag-neo4j-eval/`)
* Latency trade-off после uplift:
  `latency_p95_ms` вырос до `133.00` (baseline hard-run: `70.50`).

## Update (same date, KAG Neo4j latency tuning)

* `query_kag_neo4j` дополнительно оптимизирован:
  * lexical path: `fulltext -> limited scan fallback` (scan только если fulltext пуст),
  * lexical gating: выполняется только при `direct_rows < topk`,
  * expansion отключен для single-token query.
* Новый hard benchmark run:
  `runs/20260222_171240-kag-neo4j-eval/`.
* Результат tuning (`topk=5`, `query_count=8`):
  * `mean_recall_at_k=1.0000`
  * `mean_mrr_at_k=0.8438`
  * `hit_rate_at_k=1.0000`
  * `latency_p95_ms=66.14` (улучшение против post-uplift `133.00`).

## Update (same date, KAG Neo4j rank uplift for `star`)

* В `src/kag/neo4j_backend.py` добавлен post-merge lexical alignment bonus для single-token query:
  * `+0.35` при lexical match query token в `doc_id/title`,
  * доп. `+0.05` для canonical chapter path (`:chapters/chapter_`).
* Добавлен regression test:
  `tests/test_kag_neo4j_backend.py::test_query_kag_neo4j_single_token_prefers_chapter_filename_match`.
* Выполнен hard benchmark run:
  `runs/20260222_213235-kag-neo4j-eval/`.
* Результат:
  * `star.first_hit_rank: 4 -> 1`
  * `mean_mrr_at_k: 0.8438 -> 0.9375`
  * `hit_rate_at_k=1.0000`, `latency_p95_ms=96.84`.

## Update (same date, KAG Neo4j latency retuning after `star` uplift)

* В `query_kag_neo4j` добавлен scan-fallback gating:
  * multi-token: fallback scan выполняется только при `direct_rows=0`,
  * single-token: fallback scan выполняется только если direct hits не имеют lexical alignment по `title/doc_id`.
* В lexical matching для `doc_id` исключен namespace-prefix (`ftbquests:`), чтобы избежать ложных бонусов по generic-token запросам (например, `quest`).
* Добавлены regression tests:
  * `test_query_kag_neo4j_skips_scan_fallback_for_multi_token_with_direct_hits`
  * `test_query_kag_neo4j_skips_scan_fallback_for_single_token_with_aligned_direct_hit`
* Eval runs:
  * sample: `runs/20260222_214352-kag-neo4j-eval_01/`
    (`recall@5=1.0000`, `mrr@5=0.8333`, `hit-rate@5=1.0000`, `latency_p95_ms=94.89`)
  * hard: `runs/20260222_214352-kag-neo4j-eval/`
    (`recall@5=1.0000`, `mrr@5=0.9375`, `hit-rate@5=1.0000`, `latency_p95_ms=83.68`).

## Update (same date, KAG Neo4j eval warmup path)

* В `scripts/eval_kag_neo4j.py` добавлен CLI/runner параметр `--warmup-runs`:
  * warmup выполняет `N` прогревочных проходов по eval queries,
  * warmup-запросы не учитываются в метриках `eval_results.json`,
  * информация о warmup записывается в `run.json` (`params.warmup_runs`, `warmup.executed_calls`).
* Добавлен тест:
  `tests/test_eval_kag_neo4j.py::test_run_eval_kag_neo4j_warmup_runs_do_not_affect_metrics`.
* Последовательные прогон с `--warmup-runs 1`:
  * sample: `runs/20260222_215053-kag-neo4j-eval/`
    (`recall@5=1.0000`, `mrr@5=0.8333`, `hit-rate@5=1.0000`, `latency_p95_ms=72.10`)
  * hard: `runs/20260222_215058-kag-neo4j-eval/`
    (`recall@5=1.0000`, `mrr@5=0.9375`, `hit-rate@5=1.0000`, `latency_p95_ms=92.41`, `star.first_hit_rank=1`).
* A/B stability check (hard set, 3x runs):
  * without warmup (`--warmup-runs 0`): `avg_p95=85.98`, `min=75.22`, `max=102.59`
  * with warmup (`--warmup-runs 1`): `avg_p95=76.25`, `min=67.51`, `max=88.55`
  * quality stable in both groups: `mrr@5=0.9375`.
* Добавлен mini-benchmark script:
  `scripts/compare_kag_neo4j_warmup.py` (baseline/candidate warmup profiles + aggregate delta report).
* Smoke run:
  `runs/20260222_215707-kag-neo4j-warmup-compare/`
  (`p95_delta_ms=-28.57`, `p95_improvement_ms=28.57`, `p95_improvement_pct=33.92`).
* Runbook обновлен:
  nightly hard benchmark profile закреплен с `--warmup-runs 1`,
  добавлена команда warmup A/B compare.

## Update (same date, Automation dry-run scaffold)

* Добавлен безопасный entrypoint:
  `scripts/automation_dry_run.py` (`--plan-json`).
* Контракт: script только валидирует/нормализует action-plan и пишет artifacts;
  реальные keyboard/mouse события не выполняются (`dry_run=true`).
* Артефакты:
  `runs/<timestamp>-automation-dry-run/{run.json,actions_normalized.json,execution_plan.json}`.
* Добавлены тесты:
  `tests/test_automation_dry_run.py` (`success`, `invalid payload`, `CLI --help`).
* Smoke run:
  `runs/20260222_220630-automation-dry-run/`
  (`action_count=3`, `step_count=4`, `estimated_total_duration_ms=590`).
* Runbook/Plans/TODO синхронизированы под новый dry-run automation path.

## Session close snapshot (2026-02-22)

* Canonical sample benchmark run:
  `runs/20260222_171620-kag-neo4j-eval/`
  (`recall@5=1.0000`, `mrr@5=0.8571`, `hit-rate@5=1.0000`, `latency_p95_ms=70.96`).
* Canonical hard-cases benchmark run (post-latency-tuning):
  `runs/20260222_171240-kag-neo4j-eval/`
  (`recall@5=1.0000`, `mrr@5=0.8438`, `hit-rate@5=1.0000`, `latency_p95_ms=66.14`).
* Regression ladder (for context):
  `runs/20260222_170006-kag-neo4j-eval/` -> `runs/20260222_170453-kag-neo4j-eval/` -> `runs/20260222_171240-kag-neo4j-eval/`.

## Update (same date, docs governance refactor)

* Выполнен быстрый аудит `TODO.md` и `PLANS.md`, структура унифицирована через `docs/SOURCE_OF_TRUTH.md`:
  * `TODO.md` — execution-only (Now/Next/Blocked/Done this week, WIP-limit=3)
  * `PLANS.md` — goals/milestones/DoD/risks
  * archived/recoverable направления вынесены в `docs/ARCHIVED_TRACKS.md`
* После рефактора документации выполнен полный regression check:
  `python -m pytest` -> `136 passed`.
