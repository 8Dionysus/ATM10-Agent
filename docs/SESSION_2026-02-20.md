# SESSION_2026-02-20

A brief summary of the key results of the session dated **2026-02-20**.

## What has been done

* A two-stage pipeline (`candidate-k` + optional reranker) has been implemented for retrieval.
* For `qwen3`, runtime switch `torch|openvino` and device `AUTO|CPU|GPU|NPU` have been added.
* Added wrapper for Windows: `scripts/run_qwen3_openvino.ps1`.
* For Phase A, a real VLM provider (`openai`) has been added via the existing interface
  with `auto|stub|openai` and fallback modes in stub.
* SNBT extraction has been expanded in ingest FTB Quests:
  * quoted and unquoted key values ​​are supported
  * keys: `id`, `type`, `dimension`, `structure`, `filename`, `group`, `title`, `subtitle`, `description`.
* First-stage ranking in retrieval has been improved:
  * field-weighted scoring (`title/text/tags`)
  * stopword filtering to reduce false matches based on function words.
* Fixed stack `Qwen3` + policy `OpenVINO-first` in `docs/QWEN3_MODEL_STACK.md`.
* For `Qwen3-VL-4B-Instruct`, custom OpenVINO export path has been implemented and confirmed:
  `scripts/export_qwen3_custom_openvino.py` -> `models/qwen3-vl-4b-instruct-ov-custom`
  (artifact: `runs/20260220_150028-qwen3-custom-export/`).
* For `Qwen3-ASR-0.6B`, execute-path + `diagnostic` in `run.json` has been added to the custom exporter
  (example artifact: `runs/20260220_160146-qwen3-custom-export/`).
* Scaffold entrypoint added for `Qwen3-TTS-12Hz-0.6B-CustomVoice`
  `scripts/export_qwen3_tts_openvino.py` with dry-run/execute artifacts
  (example artifact: `runs/20260220_155424-qwen3-tts-export/`).
* Added compatibility-layer for `optimum` API shifts in VL custom export
  (`Qwen3VLOpenVINOConfig|Qwen2VLOpenVINOConfig|QwenVLOpenVINOConfig` + behavior enum variants).
* Added lazy-import `transformers.AutoConfig` for TTS probe so that in case of version mismatch
  receive a diagnosable error in `run.json`, rather than import-time crash.
* An isolated experiment was carried out in `.venv-exp` with nightly branches
  `transformers/optimum/optimum-intel` (artifact folder: `runs/20260220_190319-qwen3-exp-venv-probe/`):
  `qwen3_asr`/`qwen3_tts` are still not recognized in `AutoConfig`.
* Added long-lived voice runtime:
  * `scripts/voice_runtime_service.py` (HTTP service with in-memory ASR/TTS)
  * `scripts/voice_runtime_client.py` (thin CLI client for health/asr/tts)
* Added tests for long-lived voice runtime:
  * `tests/test_voice_runtime_service.py`
  * `tests/test_voice_runtime_client.py`
* Conducted latency benchmark voice runtime:
  * service path: `runs/20260220_211505-voice-latency-bench/latency_summary.json`
  * one-shot path: `runs/20260220_211708-voice-latency-oneshot-bench/latency_oneshot_summary.json`

## Voice latency (in-game SLA check)

Service mode (`voice_runtime_service + voice_runtime_client`):

* ASR cold: `9.141s`; warm p50/p95: `0.775s / 0.847s`
* TTS cold: `24.618s`; warm p50/p95: `9.688s / 14.747s`

One-shot mode (`asr_demo` / `tts_demo`):

* ASR p50/p95: `13.352s / 14.016s`
* TTS p50/p95: `22.445s / 24.169s`

Conclusion: service mode is significantly faster than one-shot, but in-game SLA `<=2s` on the current CPU runtime is performed only for ASR; bottleneck - `Qwen3-TTS`.

## Fixed defaults

By grid-eval (`runs/20260220_m2_calibration_none/`) production defaults are confirmed:

* `topk=5`
* `candidate_k=50`
* `reranker=none`

## Metrics (ATM10 chapters eval)

Set: `runs/20260220_m2_baseline/eval_cases_atm10_chapters.jsonl`

* Before quality-tuning: `Recall@5=1.0000`, `MRR@5=0.9333`, `hit-rate@5=1.0000`
* After quality-tuning (`runs/20260220_132946/`):
  * `Recall@5=1.0000`
  * `MRR@5=1.0000`
  * `hit-rate@5=1.0000`

## Test baseline

* `python -m pytest` -> `68 passed`.

## What's important next

* Prepare a working self-conversion path for `Qwen3-ASR-0.6B`
  (current blocker: `qwen3_asr` in export flow `transformers`).
* Design a custom-export pipeline for `Qwen3-TTS-12Hz-0.6B-CustomVoice` + tokenizer.
* Add fast-TTS fallback path for game loop SLA `<=2s`, leaving `Qwen3-TTS` as optional HQ mode.
* Follow upstream support for `qwen3_asr`/`qwen3_tts` in `transformers/optimum` and
  switch to the official export path when the architectures appear in `AutoConfig`.

## Update (cleanup, same date)

* `Qwen3-TTS` has been removed from the active project stack; roadmap voice is fixed as `ASR-only` before selecting a separate fast-fallback TTS.
* TTS experiment artifacts are stored in `runs/*qwen3-tts*` as historical reference, but excluded from the operational runbook.
* Cleanup of the environment tails has been completed: `.venv-exp` has been removed, `qwen-tts` has been removed from the working `.venv`.
