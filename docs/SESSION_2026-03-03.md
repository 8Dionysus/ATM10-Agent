# SESSION_2026-03-03

Brief summary of the session from **2026-03-03** on `G3/M6.19` rollout of the new automation intent template `open_world_map`.

## What has been done

* A new template intent has been added to `scripts/intent_to_automation_plan.py`:
  * `intent_type=open_world_map`
  * keyboard-only actions: `key_tap(m) -> wait(200ms, repeats=2) -> key_tap(space)`
  * target invariants: `action_count=3`, `step_count=4` (via dry-run expansion).
* Added canonical fixture:
  * `tests/fixtures/intent_open_world_map.json` (includes `intent_id` and `trace_id`).
* Added regression tests:
  * `tests/test_intent_to_automation_plan.py` (adapter payload + planning metadata + keyboard-only action types),
  * `tests/test_automation_intent_chain_smoke.py` (e2e chain smoke for `open_world_map`).
* CI workflow `.github/workflows/pytest.yml` has been expanded by checklist `M6.19`:
  * smoke step for `runs/ci-smoke-automation-chain-open-world-map`,
  * strict contract-check step (`--expected-intent-type open_world_map`, `--require-trace-id`, `--require-intent-id`),
  * new line `intent_chain_open_world_map` to `Automation Smoke Contracts`,
  * new artifact path `runs/ci-smoke-automation-chain-open-world-map/contract_summary.json`.
* Synchronized source-of-truth docs:
  * `docs/RUNBOOK.md`
  * `docs/DECISIONS.md`
  * `TODO.md`

## Validation

Targeted:

* `python -m pytest tests/test_intent_to_automation_plan.py tests/test_automation_intent_chain_smoke.py` -> **11 passed**

Smoke + strict contract:

* `python scripts/automation_intent_chain_smoke.py --intent-json tests/fixtures/intent_open_world_map.json --runs-dir runs/ci-smoke-automation-chain-open-world-map-local` -> **exit 0**
* `python scripts/check_automation_smoke_contract.py --mode intent_chain --runs-dir runs/ci-smoke-automation-chain-open-world-map-local --min-action-count 3 --min-step-count 4 --expected-intent-type open_world_map --require-trace-id --require-intent-id --summary-json runs/ci-smoke-automation-chain-open-world-map-local/contract_summary.json` -> **status=ok**

Full regression:

* `python -m pytest` -> **355 passed**

## Status after the session

* `G3` follow-up on `M6.19` for the new `intent_type=open_world_map` is closed.
* The public contract `automation_plan_v1` has not been changed (backward-compatible extension of the `intent_type` list to `automation_intent_v1`).

---

## Update (same date): Git/Docs housekeeping (source-of-truth sync)

### What's Synchronized

* `README.md` updated to `2026-03-03`:
  * `current session snapshot` -> `docs/SESSION_2026-03-03.md`
  * `Current status` date and link to the latest snapshot.
* `MANIFEST.md` updated to `2026-03-03`:
  * `As of` date,
  * link `Session history`,
  * capability note on automation intents (`open_world_map` enabled).
* `TODO.md` is aligned to the current stage:
  * `Status Snapshot`/`Session Focus` updated to `2026-03-03`,
  * The conflict in `Next` vs `Done` by `G3 follow-up` has been resolved (only the next intent-template stage is left in `Next`).

### Branch policy (keep + document)

Non-merged branches are left without deletion and are fixed as working/historical:

* `release/wave6-security-hardening`
* `sync/docs-master-truth-20260302`
* `hotfix/g23-transition-restore`

Policy: cleanup of branches is not performed in this iteration; synchronization occurs through `master` as a single current branch.

---

## Update (same date): G2.manual UTC preflight backport on master

### What has been done

* Helper `scripts/check_gateway_sla_manual_preflight.py` from `release/wave6-security-hardening` is backported into `master`.
* Added contract-test set `tests/test_check_gateway_sla_manual_preflight.py`:
  * `allow_accounted_dispatch` if there are no runs taken into account for the UTC day;
  * `block_accounted_dispatch` when the UTC quota is exhausted;
  * `policy=fail_if_blocked` -> `exit_code=2`;
  * ignoring non-matching `event/head_branch` rows;
  * error path if `GITHUB_TOKEN` is missing;
  * CLI `--help`.
* Updated source-of-truth docs:
  * `docs/RUNBOOK.md` (new section `G2.manual: UTC preflight before manual workflow_dispatch`);
  * `docs/DECISIONS.md` (policy commit `gateway_sla_manual_preflight_v1`);
  * `TODO.md` (added done-item for `G2.manual follow-up`).

### Operational usage

* Preflight is used as a guardrail-check before manual nightly dispatch via GitHub UI.
* The side-effect free: dispatch script does not execute, it only generates a decision summary
  `allow_accounted_dispatch|block_accounted_dispatch` + `reason_codes` and `next_accounted_dispatch_at_utc`.

---

## Update (same date): G2.manual unified manual-cycle summary contract

### What has been done

* Added new helper `scripts/check_gateway_sla_manual_cycle_summary.py` with CLI:
  * `--runs-dir`,
  * `--preflight-summary-json`, `--readiness-summary-json`, `--governance-summary-json`, `--progress-summary-json`, `--transition-summary-json`,
  * `--policy report_only|fail_if_blocked`,
  * `--summary-json`.
* The machine-readable contract `gateway_sla_manual_cycle_summary_v1` is fixed:
  * required source: `preflight`,
  * optional sources: `readiness/governance/progress/transition`,
  * for each source `status=present|missing|invalid` is published,
  * `decision` is copied from preflight (`accounted_dispatch_allowed`, `decision_status`, `next_accounted_dispatch_at_utc`, `reason_codes`),
  * `observed` aggregates `readiness_status`, `governance_decision_status`, progress/transition key fields.
* Added regression tests `tests/test_check_gateway_sla_manual_cycle_summary.py` (happy/block/fail-policy/missing optional/missing-invalid preflight/CLI help).
* Updated source-of-truth docs: `docs/RUNBOOK.md`, `docs/DECISIONS.md`, `TODO.md`.

### Validation

* `python -m pytest tests/test_check_gateway_sla_manual_cycle_summary.py` -> **7 passed**
* `python scripts/check_gateway_sla_manual_cycle_summary.py --runs-dir runs --policy report_only --summary-json runs/nightly-gateway-sla-manual-cycle/manual_cycle_summary.json` -> **exit 0**, `decision_status=block_accounted_dispatch`
* `python -m pytest` -> **368 passed**

---

## Update (same date): G2.manual local manual nightly runner (solo+AI)

### What has been done

* Added wrapper `scripts/run_gateway_sla_manual_nightly.py`:
  * local UTC guardrail (`max 1 accounted run/day`) by history `runs/nightly-gateway-sla-manual-runner/*/run.json`;
  * local preflight summary in contract `gateway_sla_manual_preflight_v1` (`inputs.preflight_source=local_artifact`);
  * execution modes: `accounted|recovery|blocked|error`;
  * fail-fast policy at chain steps;
  * recovery-path without progression credit (`transition + manual_cycle_summary`).
* Added final runner contract `gateway_sla_manual_nightly_runner_v1` to `manual_nightly_summary.json`.
* Added tests: `tests/test_run_gateway_sla_manual_nightly.py` (accounted, blocked, fail_if_blocked, recovery, fail-fast, quota counting semantics, CLI help).
* Updated docs: `docs/RUNBOOK.md`, `docs/DECISIONS.md`, `TODO.md`.

### Validation

* `python -m pytest tests/test_run_gateway_sla_manual_nightly.py` -> **7 passed**
* `python scripts/run_gateway_sla_manual_nightly.py --runs-dir runs --policy report_only --summary-json runs/nightly-gateway-sla-manual-runner/manual_nightly_summary.json` -> **exit 0**, `execution_mode=accounted`, `decision_status=allow_accounted_dispatch`
* `python scripts/check_gateway_sla_manual_cycle_summary.py --runs-dir runs --preflight-summary-json runs/nightly-gateway-sla-preflight/local_preflight_summary.json --policy report_only --summary-json runs/nightly-gateway-sla-manual-cycle/manual_cycle_summary.json` -> **exit 0**, `decision_status=allow_accounted_dispatch`
* `python -m pytest` -> **375 passed**

---

## Update (same date): G2.manual local cadence brief (daily operator status/ETA)

### What has been done

* Added new read-only helper `scripts/check_gateway_sla_manual_cadence_brief.py`.
* The machine-readable contract `gateway_sla_manual_cadence_brief_v1` is fixed:
  * required sources: `manual_cycle`, `progress`;
  * optional sources: `manual_runner`, `readiness`, `governance`, `transition`;
  * `sources.*.status = present|missing|invalid`;
  * `attention_state = source_repair_required|wait_for_utc_reset|run_recovery_only|ready_for_accounted_run|unknown`;
  * deterministic UTC forecast (`next_accounted_dispatch_at_utc`, `earliest_window_ready_at_utc`, `earliest_streak_ready_at_utc`, `earliest_go_candidate_at_utc`).
* Added tests: `tests/test_check_gateway_sla_manual_cadence_brief.py` (happy, blocked, recovery, missing required, fail-policy/report-only behavior, malformed required source, CLI help).
* Updated docs: `docs/RUNBOOK.md`, `docs/DECISIONS.md`, `TODO.md`.

### Validation

* `python -m pytest tests/test_check_gateway_sla_manual_cadence_brief.py` -> **8 passed**
* `python scripts/check_gateway_sla_manual_cadence_brief.py --runs-dir runs --policy report_only --summary-json runs/nightly-gateway-sla-manual-cadence/cadence_brief.json` -> **exit 0**, `attention_state=ready_for_accounted_run`, `decision_status=allow_accounted_dispatch`
* `python -m pytest` -> **383 passed**

---

## Update (same date): G2 Conservative Gate plan implementation (Phase 0 baseline lock)

### What is recorded

* In the source-of-truth docs execution policy `G2-only until go/no-go` is recorded (without parallel `G3/G5` execution-track):
  * `TODO.md` (Now/Next + gate-window focus);
  * `docs/RUNBOOK.md` (daily conservative loop + fallback + gate criteria);
  * `docs/DECISIONS.md` (policy fix conservative switch rules + nightly_only surface).
* Baseline lock according to latest G2 summaries at the time of launch:
  * `readiness_summary.json`: `readiness_status=not_ready`, `window_observed=2`, `critical_count=0`, `warn_ratio=0.0`;
  * `governance_summary.json`: `decision_status=hold`, `latest_ready_streak=0`, `invalid_or_mismatched_count=0`;
  * `progress_summary.json`: `decision_status=hold`, `remaining_for_window=12`, `remaining_for_streak=3`;
  * `transition_summary.json`: `allow_switch=false`, `decision_status=hold`.
* For operator-loop baseline, `cadence_brief` is fixed:
  * `attention_state=ready_for_accounted_run`;
  * `forecast.earliest_go_candidate_at_utc=2026-03-14T16:56:45.954644+00:00`.
* Gate-review window fixed as `2026-03-14/15`:
  * manual cadence earliest candidate: **2026-03-14**;
  * practical cron-based decision-run (`03:35 UTC`) earliest: **2026-03-15**.

### Validation

* `python scripts/check_gateway_sla_manual_cadence_brief.py --runs-dir runs --policy report_only --summary-json runs/nightly-gateway-sla-manual-cadence/cadence_brief.json` -> **exit 0**
* `python -m pytest -q` -> **383 passed**

---

## Update (same date): G2 manual switch override to stable nightly strict gate

### What's changed

* Based on an explicit operator request, a switch was executed in stable nightly strict mode:
  * in `.github/workflows/gateway-sla-readiness-nightly.yml` step
    `Smoke - Gateway SLA trend snapshot (fail_nightly strict gate)` now runs **without condition** `allow_switch`.
* Removed workflow step `Resolve - Gateway SLA transition gate` as it is no longer needed for the conditional execution of the strict step.
* Explicit fields have been added to the nightly transition summary:
  * `strict_gate_executed: true`
  * `strict_gate_policy: fail_nightly`
* `tests/test_gateway_sla_readiness_nightly_workflow.py` updated for new contract:
  * checks for the absence of conditional `if: env.GATEWAY_SLA_ALLOW_SWITCH == 'True'`;
  * checks for the absence of step `Resolve - Gateway SLA transition gate`;
  * checks that strict gate step is present as unconditional.
* Source-of-truth docs synchronized:
  * `docs/RUNBOOK.md` (G2.3 integration and post-switch monitoring),
  * `docs/DECISIONS.md` (manual override policy),
  * `TODO.md` (current focus: strict monitoring/remediation).

### Policy after switch

* Enforcement surface: nightly workflow only (`nightly_only`).
* `pytest.yml` remains `signal_only`.
* `readiness/governance/progress/transition` continues to work as telemetry and reason-code layer for triage/remediation.

### Validation

* `python -m pytest tests/test_gateway_sla_readiness_nightly_workflow.py` -> **passed**
* `python -m pytest` -> **383 passed**
