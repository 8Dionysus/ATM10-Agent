# SESSION_2026-03-12

Краткий конспект сессии от **2026-03-12** по `G2.5` nightly integration для remediation snapshot и финальному docs sync.

## Что сделано

* В `.github/workflows/gateway-sla-readiness-nightly.yml` подключен remediation helper:
  * step `Remediation - Gateway SLA fail_nightly snapshot (report_only)`
  * latest alias: `runs/nightly-gateway-sla-remediation/remediation_summary.json`
  * cache/artifact wiring для `runs/nightly-gateway-sla-remediation`
* G2 diagnostics в nightly workflow переведены на `always()`-публикацию summary sections:
  * `readiness`
  * `governance`
  * `progress`
  * `transition`
  * `remediation`
* Summary blocks сделаны missing-safe, чтобы `GITHUB_STEP_SUMMARY` не терял triage-контекст после strict `fail_nightly`.
* Обновлен workflow regression test:
  * `tests/test_gateway_sla_readiness_nightly_workflow.py`
  * проверяет remediation step, `report_only` policy, new summary section, cache/artifact wiring и `always()` semantics
* Выполнен docs housekeeping:
  * `README.md` переведен на current snapshot `docs/SESSION_2026-03-12.md`
  * `MANIFEST.md` актуализирован на `2026-03-12`
  * `TODO.md` выровнен по текущему G2 snapshot
* Consistency review:
  * `docs/RUNBOOK.md` и `docs/DECISIONS.md` уже отражают `G2.4/G2.5`
  * дополнительный policy-edit не потребовался

## Валидация

Targeted:

* `python -m pytest tests/test_gateway_sla_readiness_nightly_workflow.py` -> **1 passed**

Smoke:

* `python scripts/check_gateway_sla_fail_nightly_remediation.py --runs-dir runs --policy report_only --summary-json runs/nightly-gateway-sla-remediation/remediation_summary.json` -> **exit 0**

Full regression:

* `python -m pytest` -> **389 passed**

## Статус после сессии

* Human-facing current snapshot перенесен на `docs/SESSION_2026-03-12.md`.
* Workflow-published `runs/nightly-gateway-sla-remediation/remediation_summary.json` зафиксирован как source-of-truth для nightly triage; прямой локальный запуск helper остается fallback path.
* Latest local remediation snapshot по-прежнему показывает `3` candidate buckets на текущем G2 baseline:
  * `regression_investigation`
  * `window_accumulation`
  * `ready_streak_stabilization`

## Update (same date): Docs sync

### Что синхронизировано

* `README.md`:
  * `current session snapshot` -> `docs/SESSION_2026-03-12.md`
  * `Current status` дата обновлена на `2026-03-12`
* `MANIFEST.md`:
  * `Актуально на` -> `2026-03-12`
  * `Session history` -> `docs/SESSION_2026-03-12.md`
  * capability list дополнен remediation helper/nightly remediation integration
* `TODO.md`:
  * `Status Snapshot`/`Session Focus` обновлены на `2026-03-12`
  * текущий `G2 remediation loop` оставлен как workflow-first path без изменения WIP-фокуса

### Что не меняли

* `PLANS.md` не обновлялся: цели, milestones и DoD в этой сессии не менялись.
* Исторические `docs/SESSION_*.md` не переписывались.

## Update (same date): G2.post2 Streamlit remediation visibility

### Что сделано

* В `scripts/streamlit_operator_panel.py` добавлен новый optional loader для workflow-published remediation snapshot:
  * canonical path: `runs/nightly-gateway-sla-remediation/remediation_summary.json`
  * supported contract: `gateway_sla_fail_nightly_remediation_v1`
  * tolerant policy: missing -> `not available yet`, parse/schema issue -> warning без падения UI
* Во вкладке `Latest Metrics` добавлен блок `Gateway fail_nightly remediation`:
  * compact summary row по `status/policy/observed`
  * machine-readable triage fields `candidate_item_count`, `candidate_item_ids`, `reason_codes`
  * отдельная compact table по `candidate_items` (`id`, `priority`, `summary`, `source_refs`)
  * artifact panel с `checked_at_utc` и `summary_json`
* В `scripts/streamlit_operator_panel_smoke.py` remediation snapshot добавлен в optional-source semantics:
  * `streamlit_smoke_summary_v1` schema не менялся
  * отсутствие remediation summary не переводит smoke в error-path
* Обновлены regression tests:
  * `tests/test_streamlit_operator_panel.py`
  * `tests/test_streamlit_operator_panel_smoke.py`
* Обновлены operator docs:
  * `docs/RUNBOOK.md`
  * `TODO.md`

### Валидация

Targeted:

* `python -m pytest tests/test_streamlit_operator_panel.py` -> **26 passed**
* `python -m pytest tests/test_streamlit_operator_panel_smoke.py` -> **8 passed**

Full regression:

* `python -m pytest` -> **394 passed**

Live smoke:

* `python scripts/streamlit_operator_panel_smoke.py --panel-runs-dir runs --runs-dir runs/ci-smoke-streamlit --summary-json runs/ci-smoke-streamlit/streamlit_smoke_summary.json --gateway-url http://127.0.0.1:8770 --startup-timeout-sec 45 --viewport-width 390 --viewport-height 844 --compact-breakpoint-px 768` -> **exit 0**
* Latest smoke summary reports:
  * `status=ok`
  * `startup_ok=true`
  * `mobile_layout_contract_ok=true`
  * `optional_missing_sources_count=0`

### Статус после обновления

* Workflow-published remediation snapshot теперь виден не только в nightly summary/artifacts, но и в Streamlit `Latest Metrics`.
* Existing `Gateway fail_nightly progress` блок сохранен; remediation visibility добавлена аддитивно поверх текущего G2 monitoring surface.
* Human-facing current snapshot остается на `docs/SESSION_2026-03-12.md`; entry docs (`README.md`, `MANIFEST.md`) дополнительно обновлены, чтобы короткий status/capabilities surface тоже отражал Streamlit remediation visibility.

## Update (same date): G2.post3 integrity snapshot

### Что сделано

* Добавлен новый read-only helper `scripts/check_gateway_sla_fail_nightly_integrity.py`:
  * contract: `gateway_sla_fail_nightly_integrity_v1`
  * latest alias: `runs/nightly-gateway-sla-integrity/integrity_summary.json`
  * history copy: `runs/nightly-gateway-sla-integrity/<timestamp>-gateway-sla-fail-integrity/integrity_summary.json`
* Integrity snapshot проверяет единый набор G2 invariants:
  * required source health для `readiness/governance/progress/transition/remediation`
  * telemetry counters `invalid_or_mismatched_count == 0`
  * dual-write / anti-double-count invariants по `run_json` и `history_summary_json`
  * UTC guardrail consistency по `manual_cadence`, когда optional source доступен
* В `.github/workflows/gateway-sla-readiness-nightly.yml` добавлены:
  * step `Integrity - Gateway SLA fail_nightly invariants (report_only)`
  * summary section `Gateway SLA Fail-Nightly Integrity`
  * cache/artifact wiring для `runs/nightly-gateway-sla-integrity`
* В `scripts/streamlit_operator_panel.py` во вкладке `Latest Metrics` добавлен блок `Gateway fail_nightly integrity`:
  * verdict row по `integrity_status/telemetry_ok/dual_write_ok/anti_double_count_ok/utc_guardrail_status`
  * JSON panel по `utc_guardrail`
  * artifact panel с `checked_at_utc` и `summary_json`
* В `scripts/streamlit_operator_panel_smoke.py` integrity snapshot добавлен в optional-source semantics.
* Обновлены operator docs:
  * `docs/RUNBOOK.md`
  * `docs/DECISIONS.md`
  * `TODO.md`
  * `README.md`
  * `MANIFEST.md`

### Валидация

Targeted:

* `python -m pytest tests/test_check_gateway_sla_fail_nightly_integrity.py tests/test_gateway_sla_readiness_nightly_workflow.py tests/test_streamlit_operator_panel.py tests/test_streamlit_operator_panel_smoke.py` -> **44 passed**

Live run:

* `python scripts/check_gateway_sla_fail_nightly_integrity.py --runs-dir runs --policy report_only --summary-json runs/nightly-gateway-sla-integrity/integrity_summary.json` -> **exit 0**
* Latest local integrity snapshot reports:
  * `integrity_status=clean`
  * `telemetry_ok=true`
  * `dual_write_ok=true`
  * `anti_double_count_ok=true`
  * `utc_guardrail_status=ok`

Full regression:

* `python -m pytest` -> **403 passed**

### Статус после обновления

* Для `G2 telemetry integrity` теперь есть единый machine-readable source-of-truth: `runs/nightly-gateway-sla-integrity/integrity_summary.json`.
* Nightly triage получил отдельный integrity verdict без добавления нового hard fail surface сверх strict `fail_nightly` trend gate.
* Human-facing entry docs и session snapshot выровнены по состоянию после `G2.post3`.
