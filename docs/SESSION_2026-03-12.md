# SESSION_2026-03-12

Brief summary of the session from **2026-03-12** on `G2.5` nightly integration for remediation snapshot and final docs sync.

## What has been done

* The remediation helper is connected to `.github/workflows/gateway-sla-readiness-nightly.yml`:
  * step `Remediation - Gateway SLA fail_nightly snapshot (report_only)`
  * latest alias: `runs/nightly-gateway-sla-remediation/remediation_summary.json`
  * cache/artifact wiring for `runs/nightly-gateway-sla-remediation`
* G2 diagnostics in nightly workflow have been transferred to the `always()` publication summary sections:
  * `readiness`
  * `governance`
  * `progress`
  * `transition`
  * `remediation`
* Summary blocks are made missing-safe so that `GITHUB_STEP_SUMMARY` does not lose triage context after strict `fail_nightly`.
* Updated workflow regression test:
  * `tests/test_gateway_sla_readiness_nightly_workflow.py`
  * checks remediation step, `report_only` policy, new summary section, cache/artifact wiring and `always()` semantics
* Completed docs housekeeping:
  * `README.md` transferred to current snapshot `docs/SESSION_2026-03-12.md`
  * `MANIFEST.md` updated to `2026-03-12`
  * `TODO.md` is aligned to the current G2 snapshot
* Consistency review:
  * `docs/RUNBOOK.md` and `docs/DECISIONS.md` already reflect `G2.4/G2.5`
  * no additional policy-edit was required

## Validation

Targeted:

* `python -m pytest tests/test_gateway_sla_readiness_nightly_workflow.py` -> **1 passed**

Smoke:

* `python scripts/check_gateway_sla_fail_nightly_remediation.py --runs-dir runs --policy report_only --summary-json runs/nightly-gateway-sla-remediation/remediation_summary.json` -> **exit 0**

Full regression:

* `python -m pytest` -> **389 passed**

## Status after the session

* Human-facing current snapshot moved to `docs/SESSION_2026-03-12.md`.
* Workflow-published `runs/nightly-gateway-sla-remediation/remediation_summary.json` is fixed as source-of-truth for nightly triage; direct local launch of helper remains fallback path.
* Latest local remediation snapshot still shows `3` candidate buckets on the current G2 baseline:
  * `regression_investigation`
  * `window_accumulation`
  * `ready_streak_stabilization`

## Update (same date): Docs sync

### What's Synchronized

* `README.md`:
  * `current session snapshot` -> `docs/SESSION_2026-03-12.md`
  * `Current status` date updated to `2026-03-12`
* `MANIFEST.md`:
  * `As of` -> `2026-03-12`
  * `Session history` -> `docs/SESSION_2026-03-12.md`
  * capability list added remediation helper/nightly remediation integration
* `TODO.md`:
  * `Status Snapshot`/`Session Focus` updated to `2026-03-12`
  * current `G2 remediation loop` left as workflow-first path without changing WIP focus

### What didn't change

* `PLANS.md` was not updated: goals, milestones and DoD did not change in this session.
* Historical `docs/SESSION_*.md` were not rewritten.

## Update (same date): G2.post2 Streamlit remediation visibility

### What has been done

* A new optional loader for workflow-published remediation snapshot has been added to `scripts/streamlit_operator_panel.py`:
  * canonical path: `runs/nightly-gateway-sla-remediation/remediation_summary.json`
  * supported contract: `gateway_sla_fail_nightly_remediation_v1`
  * tolerant policy: missing -> `not available yet`, parse/schema issue -> warning without UI crash
* In the `Latest Metrics` tab, the `Gateway fail_nightly remediation` block has been added:
  * compact summary row by `status/policy/observed`
  * machine-readable triage fields `candidate_item_count`, `candidate_item_ids`, `reason_codes`
  * separate compact table for `candidate_items` (`id`, `priority`, `summary`, `source_refs`)
  * artifact panel with `checked_at_utc` and `summary_json`
* In `scripts/streamlit_operator_panel_smoke.py` remediation snapshot is added to optional-source semantics:
  * `streamlit_smoke_summary_v1` schema has not changed
  * lack of remediation summary does not translate smoke into error-path
* Updated regression tests:
  * `tests/test_streamlit_operator_panel.py`
  * `tests/test_streamlit_operator_panel_smoke.py`
* Updated operator docs:
  * `docs/RUNBOOK.md`
  * `TODO.md`

### Validation

Targeted:

* `python -m pytest tests/test_streamlit_operator_panel.py` -> **26 passed**
* `python -m pytest tests/test_streamlit_operator_panel_smoke.py` -> **8 passed**

Full regression:

* `python -m pytest` -> **394 passed**

Live smoke:

* `python scripts/streamlit_operator_panel_smoke.py --panel-runs-dir runs --runs-dir runs/ci-smoke-streamlit --summary-json runs/ci-smoke-streamlit/streamlit_smoke_summary.json --gateway-url http://127.0.0.1:8770 --startup-timeout-sec 45 --viewport-width 390 --viewport-height 844 --compact-breakpoint-px 768` -> **exit 0**
* Latest smoke summary reports:
  * `status=ok`
  * `startup_ok=true`
  * `mobile_layout_contract_ok=true`
  * `optional_missing_sources_count=0`

### Status after update

* Workflow-published remediation snapshot is now visible not only in nightly summary/artifacts, but also in Streamlit `Latest Metrics`.
* Existing `Gateway fail_nightly progress` block saved; remediation visibility added additively on top of the current G2 monitoring surface.
* Human-facing current snapshot remains at `docs/SESSION_2026-03-12.md`; entry docs (`README.md`, `MANIFEST.md`) have been further updated so that the short status/capabilities surface also reflects Streamlit remediation visibility.

## Update (same date): G2.post3 integrity snapshot

### What has been done

* Added new read-only helper `scripts/check_gateway_sla_fail_nightly_integrity.py`:
  * contract: `gateway_sla_fail_nightly_integrity_v1`
  * latest alias: `runs/nightly-gateway-sla-integrity/integrity_summary.json`
  * history copy: `runs/nightly-gateway-sla-integrity/<timestamp>-gateway-sla-fail-integrity/integrity_summary.json`
* Integrity snapshot checks a single set of G2 invariants:
  * required source health for `readiness/governance/progress/transition/remediation`
  * telemetry counters `invalid_or_mismatched_count == 0`
  * dual-write / anti-double-count invariants by `run_json` and `history_summary_json`
  * UTC guardrail consistency by `manual_cadence` when optional source is available
* Added to `.github/workflows/gateway-sla-readiness-nightly.yml`:
  * step `Integrity - Gateway SLA fail_nightly invariants (report_only)`
  * summary section `Gateway SLA Fail-Nightly Integrity`
  * cache/artifact wiring for `runs/nightly-gateway-sla-integrity`
* In `scripts/streamlit_operator_panel.py`, in the `Latest Metrics` tab, the `Gateway fail_nightly integrity` block has been added:
  * verdict row by `integrity_status/telemetry_ok/dual_write_ok/anti_double_count_ok/utc_guardrail_status`
  * JSON panel by `utc_guardrail`
  * artifact panel with `checked_at_utc` and `summary_json`
* In `scripts/streamlit_operator_panel_smoke.py` integrity snapshot is added to optional-source semantics.
* Updated operator docs:
  * `docs/RUNBOOK.md`
  * `docs/DECISIONS.md`
  * `TODO.md`
  * `README.md`
  * `MANIFEST.md`

### Validation

Targeted:

* `python -m pytest tests/test_check_gateway_sla_fail_nightly_integrity.py tests/test_gateway_sla_readiness_nightly_workflow.py tests/test_streamlit_operator_panel.py tests/test_streamlit_operator_panel_smoke.py` -> **44 passed**

Live run:

* `python scripts/check_gateway_sla_fail_nightly_integrity.py --runs-dir runs --policy report_only --summary-json runs/nightly-gateway-sla-integrity/integrity_summary.json` -> **exit 0**
* Latest local integrity snapshot reports:
  * `integrity_status=clean`
  * `telemetry_ok=true`
  * `dual_write_ok=true`
  * `anti_double_count_ok=true`
  * `utc_guardrail_status=ok`

Full regression:

* `python -m pytest` -> **403 passed**

### Status after update

* For `G2 telemetry integrity` there is now a single machine-readable source-of-truth: `runs/nightly-gateway-sla-integrity/integrity_summary.json`.
* Nightly triage received a separate integrity verdict without adding a new hard fail surface beyond the strict `fail_nightly` trend gate.
* Human-facing entry docs and session snapshot are aligned to the state after `G2.post3`.
