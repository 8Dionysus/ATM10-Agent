# SESSION_2026-02-24

Краткий конспект сессии от **2026-02-24** (audit-fixes pass).

## Что сделано

* Закрыт security-риск в `scripts/voice_runtime_service.py`:
  * `out_wav_path` ограничен до безопасного имени файла (без absolute/path traversal).
  * TTS output всегда записывается только в server run artifact: `runs/<timestamp>-voice-service/tts_outputs/`.
* Для совместимости обновлен `scripts/voice_runtime_client.py`:
  * клиент отправляет в `out_wav_path` только basename (без absolute path), чтобы не ломать `tts/tts-stream` на новом серверном контракте.
* Убран traceback leakage в HTTP/NDJSON ответах `voice_runtime_service`:
  * клиент получает только `error_code=internal_error` + короткий `error`.
  * traceback сохраняется локально в `service_errors.jsonl`.
* Исправлен operational дефект в `scripts/retrieve_demo.py`:
  * при backend error теперь всегда пишется `run.json` (`status=error`, `error_code=retrieval_backend_error`).
* Исправлен direct script-run path для `scripts/export_qwen3_tts_openvino.py`:
  * добавлен fallback import для `python scripts/export_qwen3_tts_openvino.py ...`.
* Исправлен flaky-case в `scripts/discover_instance.py`:
  * создание run-dir стало collision-safe при одинаковом timestamp (suffix-loop).
* Закрыт supply-chain риск в `scripts/tts_runtime_service.py` (Silero path):
  * remote `torch.hub` источник заблокирован по умолчанию;
  * для remote требуется explicit opt-in (`SILERO_ALLOW_REMOTE_HUB=true`) и pinned revision (`SILERO_REPO_REF` или `owner/repo:ref`);
  * local source path остается разрешенным без opt-in.
* Закрыт `M5.8` по KAG trend severity:
  * в `scripts/kag_guardrail_trend_snapshot.py` добавлены пороги `warn/critical` для regression deltas (`mrr`, `latency_p95_ms`);
  * `trend_snapshot.json` и `summary.md` теперь включают severity-поля (`mrr_regression_severity`, `latency_p95_regression_severity`, `max_regression_severity`);
  * пороги вынесены в CLI (`--mrr-warn-delta`, `--mrr-critical-delta`, `--latency-warn-delta-ms`, `--latency-critical-delta-ms`).
* Закрыт `M5.9` для nightly reporting:
  * в `.github/workflows/kag-neo4j-guardrail-nightly.yml` step summary дополнен severity-полями для `sample/hard` (`mrr`, `latency_p95`, `max_regression_severity`);
  * существующие поля `mrr_status`, `latency_p95_status`, `has_any_regression` сохранены для обратной совместимости summary-потребителей.
* Закрыт `M6.11` по quick-links формату:
  * в `pytest` и nightly summaries введен единый layout-блок `### Quick Links`;
  * quick-link на `RUNBOOK M6.8` выровнен по одинаковому label/markdown формату;
  * в `pytest` summary добавлен fallback path для runbook URL при отсутствии GitHub env-переменных.
* Закрыт `M6.12` по helper-конвенции для runbook links:
  * добавлен общий helper `scripts/build_runbook_link.py` (CLI + reusable function);
  * `pytest` summary и nightly trend summary переведены на единый helper вместо дублированной inline-сборки URL;
  * добавлены unit tests `tests/test_build_runbook_link.py` для env/fallback/default-ref поведения.
* Закрыт `M6.13` по planning metadata envelope:
  * в `automation_plan_v1` добавлен optional блок `planning` для trace/correlation metadata;
  * `intent_to_automation_plan` заполняет `planning` (`intent_type`, `intent_schema_version`, `adapter_name`, `adapter_version`) и пробрасывает `intent_id/trace_id` при наличии;
  * `automation_dry_run` валидирует/нормализует `planning`, а `automation_intent_chain_smoke` пробрасывает `trace_id` в `chain_summary.json` и `run.json`.
* Закрыт `M6.14` по CI smoke coverage:
  * добавлен второй intent fixture `tests/fixtures/intent_check_inventory_tool.json`;
  * `pytest` smoke workflow расширен вторым intent-chain прогоном (`check_inventory_tool`) с отдельным contract summary;
  * в summary таблицу добавлена отдельная строка для inventory-сценария.
* Синхронизирован `PLANS.md`:
  * устаревшие open tasks по `M5.8/M5.9/M6.11/M6.12/M6.13/M6.14` убраны;
  * добавлены актуальные next-level open tasks для `G1/G2/G3` (trace-correlation в CI summaries, policy по critical trend severity, обязательный machine-readable summary для новых smoke entrypoints).
* Закрыт `M6.15` по trace-correlation в contract summaries:
  * `scripts/check_automation_smoke_contract.py` теперь добавляет `trace_id/intent_id` в `summary_json.observed` (когда поля доступны);
  * для intent-chain добавлен fallback `trace_id` через `chain_summary.intent_adapter` и `run.json.result`.
* Закрыт `M6.16` по CI triage visibility:
  * в `.github/workflows/pytest.yml` таблица `Automation Smoke Contracts` расширена колонками `trace_id/intent_id`;
  * canonical fixtures (`intent_open_quest_book`, `intent_check_inventory_tool`, `automation_plan_quest_book`) дополнены `intent_id/trace_id`, чтобы summary не был пустым на базовых сценариях.
* Закрыт `M6.17` по strict trace contract-check:
  * в `scripts/check_automation_smoke_contract.py` добавлен флаг `--require-trace-id` для intent-chain проверок;
  * CI smoke validation шаги для `open_quest_book` и `check_inventory_tool` переведены на `--require-trace-id` (fail-fast при отсутствии trace id).
* Закрыт `M6.18` по strict intent-id contract-check:
  * в `scripts/check_automation_smoke_contract.py` добавлен флаг `--require-intent-id` для intent-chain проверок;
  * CI smoke validation шаги для `open_quest_book` и `check_inventory_tool` переведены на `--require-intent-id` (fail-fast при отсутствии intent id).
* Закрыт `M6.19` по policy formalization:
  * в `docs/RUNBOOK.md` добавлен отдельный checklist `New intent template rollout policy (CI)`;
  * checklist фиксирует обязательные шаги: fixture, smoke run, strict contract-check, summary/artifact wiring, regression test.
* Для `G3` добавлен machine-readable summary слой для core smoke entrypoints:
  * добавлен `scripts/collect_smoke_run_summary.py` для `phase_a_smoke`, `retrieve_demo`, `eval_retrieval`;
  * в `pytest` workflow добавлены validate steps с записью `runs/ci-smoke-*/smoke_summary.json`;
  * step summary дополнен таблицей `Core Smoke Summaries`, а artifact upload расширен до общего пакета `smoke-contract-summaries`.
* Для `G2` зафиксирован policy по `critical` trend severity:
  * в `scripts/kag_guardrail_trend_snapshot.py` добавлен параметр `--critical-policy` (`signal_only|fail_nightly`);
  * baseline policy для nightly зафиксирован как `signal_only` (critical severity сигнализируется в summary/artifacts и не валит job);
  * добавлен explicit opt-in режим `fail_nightly` (возвращает non-zero при critical regression) + regression test.
* Для `G2` выполнена калибровка latency severity thresholds по локальной warmup=1 истории `kag-neo4j-eval`:
  * наблюдаемый noise-floor по rolling-baseline latency regression доходил до ~`13.9 ms` при стабильном `MRR`;
  * defaults в `kag_guardrail_trend_snapshot` обновлены с `warn=2.0/critical=8.0` до `warn=5.0/critical=15.0`;
  * nightly workflow теперь явно пробрасывает эти thresholds в step `Build guardrail trend snapshot`.
* Собран отдельный calibration artifact snapshot:
  * `runs/20260224_151324-kag-threshold-calibration/{run.json,summary.json,summary.md}`;
  * в `summary.json` зафиксированы source-runs, rolling noise stats и рекомендация по thresholds/policy.

## Тесты

Точечные прогоны:

* `python -m pytest tests/test_voice_runtime_service.py`
* `python -m pytest tests/test_retrieve_demo_script.py`
* `python -m pytest tests/test_export_qwen3_tts_openvino.py`
* `python -m pytest tests/test_discover_instance.py`
* `python -m pytest tests/test_tts_runtime_service.py`
* `python -m pytest tests/test_kag_guardrail_trend_snapshot.py`
* `python -m pytest tests/test_automation_dry_run.py tests/test_intent_to_automation_plan.py tests/test_automation_intent_chain_smoke.py`
* `python -m pytest tests/test_automation_intent_chain_smoke.py`
* `python -m pytest tests/test_check_automation_smoke_contract.py`
* `python -m pytest tests/test_collect_smoke_run_summary.py`

Финальный regression-check:

* `python -m pytest` -> **186 passed**.

## Документы

* Обновлен `docs/DECISIONS.md` (раздел `2026-02-24`) с фиксацией принятых решений по security/reliability fix-pack.
* Обновлен `docs/RUNBOOK.md` (раздел long-lived voice runtime) с явным security-примечанием по `out_wav_path`.
* Обновлены `TODO.md`, `docs/RUNBOOK.md`, `docs/DECISIONS.md` под `M5.8` (severity-правила rolling-baseline regression flags).
* Обновлен `.github/workflows/kag-neo4j-guardrail-nightly.yml` под `M5.9` (severity-поля в nightly trend summary).
* Обновлены `.github/workflows/pytest.yml` и `.github/workflows/kag-neo4j-guardrail-nightly.yml` под `M6.11` (унифицированный quick-links формат).
* Обновлены `.github/workflows/pytest.yml`, `.github/workflows/kag-neo4j-guardrail-nightly.yml`, добавлены `scripts/build_runbook_link.py` и `tests/test_build_runbook_link.py` под `M6.12`.
* Обновлены `scripts/automation_dry_run.py`, `scripts/intent_to_automation_plan.py`, `scripts/automation_intent_chain_smoke.py` и тесты под `M6.13`; синхронизированы `TODO.md`, `docs/RUNBOOK.md`, `docs/DECISIONS.md`.
* Обновлены `.github/workflows/pytest.yml`, `docs/RUNBOOK.md`, `TODO.md`, `tests/test_automation_intent_chain_smoke.py`; добавлен `tests/fixtures/intent_check_inventory_tool.json` под `M6.14`.
* Обновлен `PLANS.md` (актуализированы open tasks в `G1/G2/G3`).
* Обновлены `scripts/check_automation_smoke_contract.py`, `tests/test_check_automation_smoke_contract.py`, `TODO.md`, `docs/RUNBOOK.md`, `docs/DECISIONS.md` под `M6.15`.
* Обновлены `.github/workflows/pytest.yml`, `tests/fixtures/intent_open_quest_book.json`, `tests/fixtures/intent_check_inventory_tool.json`, `tests/fixtures/automation_plan_quest_book.json`, `PLANS.md`, `TODO.md`, `docs/RUNBOOK.md`, `docs/DECISIONS.md` под `M6.16`.
* Обновлены `scripts/check_automation_smoke_contract.py`, `tests/test_check_automation_smoke_contract.py`, `.github/workflows/pytest.yml`, `PLANS.md`, `TODO.md`, `docs/RUNBOOK.md`, `docs/DECISIONS.md` под `M6.17`.
* Обновлены `scripts/check_automation_smoke_contract.py`, `tests/test_check_automation_smoke_contract.py`, `.github/workflows/pytest.yml`, `PLANS.md`, `TODO.md`, `docs/RUNBOOK.md`, `docs/DECISIONS.md` под `M6.18`.
* Обновлены `docs/RUNBOOK.md`, `PLANS.md`, `TODO.md`, `docs/DECISIONS.md` под `M6.19` (rollout policy checklist).
* Добавлены `scripts/collect_smoke_run_summary.py`, `tests/test_collect_smoke_run_summary.py`; обновлены `.github/workflows/pytest.yml`, `docs/RUNBOOK.md`, `TODO.md`, `PLANS.md`, `docs/DECISIONS.md` под `G3` (machine-readable summaries для core smoke).
* Обновлены `scripts/kag_guardrail_trend_snapshot.py`, `tests/test_kag_guardrail_trend_snapshot.py`, `.github/workflows/kag-neo4j-guardrail-nightly.yml`, `docs/RUNBOOK.md`, `PLANS.md`, `TODO.md`, `docs/DECISIONS.md` под policy `G2` (`critical_policy=signal_only|fail_nightly`).
* Обновлены `scripts/kag_guardrail_trend_snapshot.py`, `tests/test_kag_guardrail_trend_snapshot.py`, `.github/workflows/kag-neo4j-guardrail-nightly.yml`, `docs/RUNBOOK.md`, `PLANS.md`, `TODO.md`, `docs/DECISIONS.md` под калибровку latency thresholds `G2` (`warn=5.0`, `critical=15.0`).

## Закрытие сессии

* Source-of-truth документы (`TODO/PLANS/RUNBOOK/DECISIONS/SESSION`) синхронизированы.
* Финальный handoff зафиксирован в `TODO.md -> Next` через follow-up на `M6.19`.
* Regression status на момент закрытия: `python -m pytest` -> **186 passed**.
