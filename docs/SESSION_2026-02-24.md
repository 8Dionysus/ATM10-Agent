# SESSION_2026-02-24

Brief summary of the session dated **2026-02-24** (audit-fixes pass).

## What has been done

* Security risk in `scripts/voice_runtime_service.py` closed:
  * `out_wav_path` is limited to a safe filename (no absolute/path traversal).
  * TTS output is always written only to server run artifact: `runs/<timestamp>-voice-service/tts_outputs/`.
* Updated `scripts/voice_runtime_client.py` for compatibility:
  * the client sends only the basename (without absolute path) to `out_wav_path`, so as not to break `tts/tts-stream` on the new server contract.
* Removed traceback leakage in HTTP/NDJSON responses `voice_runtime_service`:
  * the client receives only `error_code=internal_error` + short `error`.
  * traceback is saved locally in `service_errors.jsonl`.
* Fixed operational defect in `scripts/retrieve_demo.py`:
  * with backend error, `run.json` (`status=error`, `error_code=retrieval_backend_error`) is now always written.
* Fixed direct script-run path for `scripts/export_qwen3_tts_openvino.py`:
  * added fallback import for `python scripts/export_qwen3_tts_openvino.py ...`.
* Fixed flaky-case in `scripts/discover_instance.py`:
  * creation of run-dir has become collision-safe with the same timestamp (suffix-loop).
* Closed supply-chain risk in `scripts/tts_runtime_service.py` (Silero path):
  * remote `torch.hub` source blocked by default;
  * remote requires explicit opt-in (`SILERO_ALLOW_REMOTE_HUB=true`) and pinned revision (`SILERO_REPO_REF` or `owner/repo:ref`);
  * local source path remains allowed without opt-in.
* Closed `M5.8` by KAG trend severity:
  * `warn/critical` thresholds for regression deltas (`mrr`, `latency_p95_ms`) have been added to `scripts/kag_guardrail_trend_snapshot.py`;
  * `trend_snapshot.json` and `summary.md` now include severity fields (`mrr_regression_severity`, `latency_p95_regression_severity`, `max_regression_severity`);
  * thresholds are included in the CLI (`--mrr-warn-delta`, `--mrr-critical-delta`, `--latency-warn-delta-ms`, `--latency-critical-delta-ms`).
* Closed `M5.9` for nightly reporting:
  * in `.github/workflows/kag-neo4j-guardrail-nightly.yml` step summary added severity fields for `sample/hard` (`mrr`, `latency_p95`, `max_regression_severity`);
  * existing fields `mrr_status`, `latency_p95_status`, `has_any_regression` are preserved for backward compatibility of summary consumers.
* Closed `M6.11` using quick-links format:
  * a single layout block `### Quick Links` has been introduced into `pytest` and nightly summaries;
  * quick-link on `RUNBOOK M6.8` is aligned to the same label/markdown format;
  * added fallback path for runbook URL in `pytest` summary in the absence of GitHub env variables.
* Closed `M6.12` according to helper convention for runbook links:
  * added general helper `scripts/build_runbook_link.py` (CLI + reusable function);
  * `pytest` summary and nightly trend summary have been moved to a single helper instead of a duplicated inline URL assembly;
  * added unit tests `tests/test_build_runbook_link.py` for env/fallback/default-ref behavior.
* Closed `M6.13` by planning metadata envelope:
  * an optional `planning` block for trace/correlation metadata has been added to `automation_plan_v1`;
  * `intent_to_automation_plan` fills `planning` (`intent_type`, `intent_schema_version`, `adapter_name`, `adapter_version`) and forwards `intent_id/trace_id` if available;
  * `automation_dry_run` validates/normalizes `planning`, and `automation_intent_chain_smoke` forwards `trace_id` to `chain_summary.json` and `run.json`.
* Closed `M6.14` for CI smoke coverage:
  * added second intent fixture `tests/fixtures/intent_check_inventory_tool.json`;
  * `pytest` smoke workflow is expanded with a second intent-chain run (`check_inventory_tool`) with a separate contract summary;
  * A separate line for the inventory scenario has been added to the summary table.
* Synchronized by `PLANS.md`:
  * outdated open tasks by `M5.8/M5.9/M6.11/M6.12/M6.13/M6.14` have been removed;
  * added current next-level open tasks for `G1/G2/G3` (trace-correlation in CI summaries, policy on critical trend severity, mandatory machine-readable summary for new smoke entrypoints).
* Closed `M6.15` for trace-correlation in contract summaries:
  * `scripts/check_automation_smoke_contract.py` now adds `trace_id/intent_id` to `summary_json.observed` (when fields are available);
  * for intent-chain, fallback `trace_id` via `chain_summary.intent_adapter` and `run.json.result` has been added.
* Closed `M6.16` by CI triage visibility:
  * in `.github/workflows/pytest.yml`, table `Automation Smoke Contracts` is expanded with `trace_id/intent_id` columns;
  * canonical fixtures (`intent_open_quest_book`, `intent_check_inventory_tool`, `automation_plan_quest_book`) have been supplemented with `intent_id/trace_id` so that the summary is not empty in basic scenarios.
* Closed `M6.17` by strict trace contract-check:
  * `--require-trace-id` flag has been added to `scripts/check_automation_smoke_contract.py` for intent-chain checks;
  * CI smoke validation steps for `open_quest_book` and `check_inventory_tool` have been transferred to `--require-trace-id` (fail-fast if there is no trace id).
* Closed `M6.18` by strict intent-id contract-check:
  * `--require-intent-id` flag has been added to `scripts/check_automation_smoke_contract.py` for intent-chain checks;
  * CI smoke validation steps for `open_quest_book` and `check_inventory_tool` have been transferred to `--require-intent-id` (fail-fast in the absence of intent id).
* Closed `M6.19` for policy formalization:
  * a separate checklist `New intent template rollout policy (CI)` has been added to `docs/RUNBOOK.md`;
  * checklist records the required steps: fixture, smoke run, strict contract-check, summary/artifact wiring, regression test.
* For `G3`, a machine-readable summary layer has been added for core smoke entrypoints:
  * added `scripts/collect_smoke_run_summary.py` for `phase_a_smoke`, `retrieve_demo`, `eval_retrieval`;
  * validate steps with entry `runs/ci-smoke-*/smoke_summary.json` have been added to `pytest` workflow;
  * The step summary has been supplemented with the `Core Smoke Summaries` table, and the artifact upload has been expanded to the general `smoke-contract-summaries` package.
* For `G2` the policy according to `critical` trend severity has been fixed:
  * added parameter `--critical-policy` (`signal_only|fail_nightly`) to `scripts/kag_guardrail_trend_snapshot.py`;
  * baseline policy for nightly is fixed as `signal_only` (critical severity is signaled in summary/artifacts and does not fail jobs);
  * added explicit opt-in mode `fail_nightly` (returns non-zero for critical regression) + regression test.
* For `G2`, latency severity thresholds were calibrated using the local warmup=1 history of `kag-neo4j-eval`:
  * the observed noise-floor by rolling-baseline latency regression reached ~`13.9 ms` with stable `MRR`;
  * defaults in `kag_guardrail_trend_snapshot` updated from `warn=2.0/critical=8.0` to `warn=5.0/critical=15.0`;
  * nightly workflow now explicitly forwards these thresholds to step `Build guardrail trend snapshot`.
* A separate calibration artifact snapshot has been collected:
  * `runs/20260224_151324-kag-threshold-calibration/{run.json,summary.json,summary.md}`;
  * in `summary.json` source-runs, rolling noise stats and thresholds/policy recommendations are recorded.

## Tests

Point runs:

* `python -m pytest tests/test_voice_runtime_service.py`
* `python -m pytest tests/test_retrieve_demo_script.py`
* `python -m pytest tests/test_export_qwen3_tts_openvino.py`
* `python -m pytest tests/test_discover_instance.py`
* `python -m pytest tests/test_tts_runtime_service.py`
* `python -m pytest tests/test_kag_guardrail_trend_snapshot.py`
* `python -m pytest tests/test_automation_dry_run.py tests/test_intent_to_automation_plan.py tests/test_automation_intent_chain_smoke.py`
* `python -m pytest tests/test_automation_intent_chain_smoke.py`
* `python -m pytest tests/test_check_automation_smoke_contract.py`
* `python -m pytest tests/test_collect_smoke_run_summary.py`

Final regression check:

* `python -m pytest` -> **186 passed**.

## Documents

* Updated `docs/DECISIONS.md` (section `2026-02-24`) with fixation of decisions made on security/reliability fix-pack.
* Updated `docs/RUNBOOK.md` (long-lived voice runtime section) with an explicit security note for `out_wav_path`.
* Updated `TODO.md`, `docs/RUNBOOK.md`, `docs/DECISIONS.md` under `M5.8` (rolling-baseline regression flags severity rules).
* Updated `.github/workflows/kag-neo4j-guardrail-nightly.yml` under `M5.9` (severity fields in nightly trend summary).
* Updated `.github/workflows/pytest.yml` and `.github/workflows/kag-neo4j-guardrail-nightly.yml` under `M6.11` (unified quick-links format).
* Updated `.github/workflows/pytest.yml`, `.github/workflows/kag-neo4j-guardrail-nightly.yml`, added `scripts/build_runbook_link.py` and `tests/test_build_runbook_link.py` under `M6.12`.
* Updated `scripts/automation_dry_run.py`, `scripts/intent_to_automation_plan.py`, `scripts/automation_intent_chain_smoke.py` and tests for `M6.13`; `TODO.md`, `docs/RUNBOOK.md`, `docs/DECISIONS.md` are synchronized.
* Updated `.github/workflows/pytest.yml`, `docs/RUNBOOK.md`, `TODO.md`, `tests/test_automation_intent_chain_smoke.py`; added `tests/fixtures/intent_check_inventory_tool.json` under `M6.14`.
* Updated `PLANS.md` (updated open tasks in `G1/G2/G3`).
* Updated `scripts/check_automation_smoke_contract.py`, `tests/test_check_automation_smoke_contract.py`, `TODO.md`, `docs/RUNBOOK.md`, `docs/DECISIONS.md` under `M6.15`.
* Updated `.github/workflows/pytest.yml`, `tests/fixtures/intent_open_quest_book.json`, `tests/fixtures/intent_check_inventory_tool.json`, `tests/fixtures/automation_plan_quest_book.json`, `PLANS.md`, `TODO.md`, `docs/RUNBOOK.md`, `docs/DECISIONS.md` under `M6.16`.
* Updated `scripts/check_automation_smoke_contract.py`, `tests/test_check_automation_smoke_contract.py`, `.github/workflows/pytest.yml`, `PLANS.md`, `TODO.md`, `docs/RUNBOOK.md`, `docs/DECISIONS.md` under `M6.17`.
* Updated `scripts/check_automation_smoke_contract.py`, `tests/test_check_automation_smoke_contract.py`, `.github/workflows/pytest.yml`, `PLANS.md`, `TODO.md`, `docs/RUNBOOK.md`, `docs/DECISIONS.md` under `M6.18`.
* Updated `docs/RUNBOOK.md`, `PLANS.md`, `TODO.md`, `docs/DECISIONS.md` under `M6.19` (rollout policy checklist).
* Added `scripts/collect_smoke_run_summary.py`, `tests/test_collect_smoke_run_summary.py`; updated `.github/workflows/pytest.yml`, `docs/RUNBOOK.md`, `TODO.md`, `PLANS.md`, `docs/DECISIONS.md` under `G3` (machine-readable summaries for core smoke).
* Updated `scripts/kag_guardrail_trend_snapshot.py`, `tests/test_kag_guardrail_trend_snapshot.py`, `.github/workflows/kag-neo4j-guardrail-nightly.yml`, `docs/RUNBOOK.md`, `PLANS.md`, `TODO.md`, `docs/DECISIONS.md` under policy `G2` (`critical_policy=signal_only|fail_nightly`).
* Updated `scripts/kag_guardrail_trend_snapshot.py`, `tests/test_kag_guardrail_trend_snapshot.py`, `.github/workflows/kag-neo4j-guardrail-nightly.yml`, `docs/RUNBOOK.md`, `PLANS.md`, `TODO.md`, `docs/DECISIONS.md` to calibrate latency thresholds `G2` (`warn=5.0`, `critical=15.0`).

## Closing the session

* Source-of-truth documents (`TODO/PLANS/RUNBOOK/DECISIONS/SESSION`) are synchronized.
* The final handoff was recorded in `TODO.md -> Next` via follow-up on `M6.19`.
* Regression status at the time of closing: `python -m pytest` -> **186 passed**.
